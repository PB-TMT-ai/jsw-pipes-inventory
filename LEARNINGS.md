# Project Learnings

Track errors, solutions, and insights. System gets smarter with each entry.

## Format
Date | Component | Issue | Resolution | Insight

---

2026-08-30 | `src/lib/calc.js` + `src/App.jsx` + `src/lib/reports.js` — Hyderabad's slit-coil count said 415.214 T, the Dashboard said 841.137 T | The plant team's `slitt_stock_28_aug.xlsx` listed **170 baby coils, 415.214 T**. The app's "Baby Coils Left" card read **841.137 T**. The instinct is to distrust the plant file; the ID-level join said the opposite — **all 170 of their coils were in `baby_coils`**, none flagged consumed, none fully used, slit weights matching to **0.113 T** in total. So the entire 425.9 T gap was tonnage the app was holding and the floor was not, and it broke into four lines with **four different causes**: 234.478 T of part-used ends across **1,430** coils averaging 0.16 T (1,163 of them under 0.1 T); 116.659 T of NPMD + Tapi coils, correctly absent because the file is Hyderabad-only; **79.910 T across 33 Hyderabad coils showing 0.000 T consumed** that the plant does not have; and −5.124 T already booked against the plant's own 170. Two of the four were the app's, and the mechanism behind the big one was the allocator itself — FIFO kept *offering* 0.1 T ends, so every run manufactured a few more. The second was quieter: an operator marking a coil **Consumed** hid it from the Production picker but **not** from the Dashboard card, which filtered on `deleted` alone — 331 coils, 17.111 T. `buildRawMaterialData` used a **third** rule (dropped `consumed`, kept every sliver) and its own header comment admitted it "may trail the Dashboard card" — three screens, three definitions, one documented as intentional. | One helper, `babyCoilIsStock` (+ `babyCoilFree` / `babyCoilStock` / `SCRAP_FREE_MT = 0.2`) in `calc.js`, read by all four sites: the Dashboard card, Coil Tracker's status and footer, the Production FIFO adapter **and** manual picker, and the Raw Material report's Strip section. The floor went in the **adapter**, ahead of `coilFifoAllocate` — putting it only on the count would have left the scrap generator running, and the allocator stays generic over any coil. `babyCoilFree` is deliberately **not** floored at zero: over-consumption must stay visible. Coil Tracker's status became **three** states (`Consumed` / `Scrap` / `Active`) with a footer printing counted tonnage beside dropped tonnage and a Status filter to list it — nothing vanishes silently, and no row is written, no flag set, no coil deleted. 12 new unit tests built on the real coil ids, **496 green**, `vite build` clean. ADR-0007 written; `ALGORITHMS.md` (adapter diagram), `UI-PATTERNS.md` and the `reports.js` header comment corrected. Live effect on the 30-Aug database: **1,099.198 T → 998.519 T**, 1,741 → 638 coils (the flag 17.109 T, the floor 83.570 T). NOT done: the 33 unaccounted coils were **not** guessed at — they are listed in `reports/Baby-Coils-Unaccounted-2026-08-30.xlsx` for the plant to answer coil by coil; no Supabase row was written; the floor was not applied to mother coils. | Three. (1) **When an outside count disagrees with the system, join on ids before arguing about totals.** Two totals 2× apart invite a hunt for a factor-of-two bug. The ID join found the plant's list *entirely correct* and turned one 425.9 T mystery into four separate findings with different owners — two code fixes, one non-issue (plant scope), one data hole for the floor. **A reconciliation that does not resolve to a line-item list is not a reconciliation, it is two opinions.** (2) **The count and the allocator have to be fixed in the same commit, because the allocator is what creates what the count carries.** A scrap floor applied only to reporting cleans the number once and lets FIFO refill it — 1,430 remnants were *made* one suggestion at a time. Ask of any "phantom inventory" fix: what process emits these, and does the fix reach it? (3) **Three screens, three definitions of the same word, and one of them documented the disagreement as deliberate.** `reports.js` said its total "may trail the Dashboard card … intentional for an available-stock sheet" — a true sentence that made a genuine defect read as a design choice, so every review since accepted it. The same shape as the 2026-08-20 entry's three captions. **A comment explaining why two numbers differ is evidence they should not**; the fix is one helper both call, not a better comment. Bonus, found by a test: `1 − 0.8` is `0.19999999999999996`, so a bare `>= 0.2` scraps a coil sitting exactly ON the floor — the compare rounds to the kilogram, which is the precision the number is stored and displayed at anyway.

2026-08-28 | `src/data/skus.js` + the live `skus` table — "add 72x72x3 in the SKU Master" | The size was a real gap: 72x72 carries ten thicknesses (1.60 … 4.80) and **3.00 was the one hole**, sitting between 2.90 (`…10059436`) and 3.20 (`…10059437`). Two traps sat in front of it. **(1) The blueprint was stale in the direction that produces a silent no-op.** `blueprints/add-new-sku-type.md` said to edit `DEFAULT_SKUS` in `src/App.jsx` — it moved to `src/data/skus.js` — and never mentioned that the Supabase `skus` table (276 rows) is what the running app reads, so the catalog file is a *fallback only*. Following it exactly would have produced a correct-looking diff, green tests, and **zero change in the live app**. **(2) `skuCode` is the ERP MM ID, not a readable string,** and the original 72x72 block `…10059431–439` has no spare code for 3.00 — inventing `SHS-72x72x3.00` would have created a SKU that never matches an ERP order or invoice by code. The real id was already in the data: `1139-13064-10080504`, on **8 order lines / 68 qty**. Querying `orders left join skus` for that one id then exposed the actual size of the problem — **47 ERP-ordered codes with no master row**, 126 qty of 100x100x3 among them. | Generated through `scripts/generate-skus.mjs` rather than by hand — the density formula is quarantined there deliberately, and it self-checks two known catalog rows before emitting. Added a dated batch to its `MISSING` array; the 2026-06-24 entries skip themselves now that they are in the catalog, so batch history is preserved rather than overwritten. 72x72x3 → `weightPerTube 38.998799999999996`, `thicknessExtra 0` (t > 2.0), `ladderPrice 2900`, `totalConversion 113.09651999999998`. Then the same pass for the rest: **42 of the 47 landed** in both `src/data/skus.js` (269 → 309 entries, header count corrected from a stale 247) and the live table (277 → 319 rows, `on conflict (sku_code) do nothing`, fresh UUID ids per `dedupe-sku-master.sql`). **5 were deliberately held**: 125 NB and 150 NB CHS (`…10080554`, `…10093949`, `…10094970/71/72`). The catalog's NB→OD map stops at 100 NB (114.3) and `parseDescription` throws rather than guess; nothing in the DB, and neither Excel, carries a 125/150 NB row to derive it from. 484 unit tests green. Blueprint rewritten to the real workflow. NOT done: the 5 held CHS rows; the pre-existing duplicate identity `chs|1161|25 nb|2.50|6000` (`…10072461` and `…10079344`, both already in HEAD) was reported, not collapsed — `scripts/dedupe-sku-master.sql` is the tool for that and it is a separate change; and no `productions`/`dispatches` data was touched. | Three. (1) **Before adding a master row, check whether the id you need is already in the transactional data.** The one input I could not invent — the ERP MM ID — was sitting in `orders.mm_id` on eight lines the whole time. The instinct is to ask the operator for a code, or worse to synthesise a readable one; the join that finds it (`orders left join skus on sku_code = mm_id`) takes ten seconds and is now step 1 of the blueprint. It also turns a one-SKU request into an accurate count of the same defect. (2) **A blueprint that names the wrong file fails silently, which is the worst way to fail.** A stale *rule* gets contradicted by the code and someone notices. A stale *procedure* completes, produces a plausible diff and green tests, and the change simply does not reach production — the two-layer catalog/table split is exactly the shape that hides this. When a blueprint's Known Issues section admits "only loads on first run", treat that as the whole story having moved, not a footnote. (3) **Prove the identity key is unchanged before adding a SKU, not after.** Orders for a missing SKU already resolve through `skuKeyResolver`'s description fallback, so adding the master row *re-routes live rows* from the desc bridge to `byCode`. If the two keys disagree the reported tonnage moves, and it moves quietly. `canonicalSkuKey(desc) === canonicalSkuKey(newObject)` — checked for all 42, all identical — is what makes "this only fills a gap" a verified claim instead of a hope.

2026-08-27 | `src/data/plants.js` + `src/lib/calc.js` — ticket #156, activating Lepakshi and Tapi | Two plants modelled for attribution only (63 and 143 order lines, never a production or an invoice) had to start running Coil Inward → Slitting → Production. **Two switches, in two files, and they are ANDed:** `manufactures` on the plant master, and the plant id on `COIL_INWARD_PLANT_IDS` — `coilInwardPlants()` intersects them, so flipping either alone leaves the picker exactly as it was, with nothing on screen to say why. The docs made this easy to get half-right: `ARCHITECTURE.md`, `DATA-MODEL.md`, `UI-PATTERNS.md`, `CONTEXT.md`, ADR-0004, ADR-0005, two blueprints and the header comments in both source files each stated some version of "Lepakshi and Tapi carry orders only / are never offered here" — roughly a dozen assertions that would have survived a correct code change and gone on telling the next reader the opposite. Blocked by #155, whose PR was still **open and unmerged** when this work started. | Branched off #155's head rather than redoing or ignoring it, so the fictional-plant fixtures were in place from the first commit and this diff touches no test that #155 had just rewritten; once its PR merged, the branch was re-based onto `staging` so it carries only its own commits. Waiting for the merge would have idled; duplicating the prefactor would have collided with it. Both flags flipped; `COIL_INWARD_PLANT_IDS` widened to all four with **Hyderabad still first**, which is the whole of why `DEFAULT_COIL_PLANT` did not move and an ordinary coil is still one click. Numbering needed nothing — `nextCoilNumber` has been per-plant and `genHRCoilId` prefix-driven since #122, and `LEP-`/`TAP-` were on the master from #119. Existing assertions **widened, never deleted**: the `manufactures`-toggle test now loops `PLANT_IDS` and checks each plant independently, and the "manufacturing but off the rollout list" half — which no real plant can demonstrate any more — moved onto a fictional master row, the same technique #155 established. `App.jsx`'s scope pre-selection comment now reads off `coilInwardPlants()` instead of naming plants. Two e2e cases added — the dropdown's exact options in order, and Lepakshi/Tapi pre-selecting when scoped (through both routes: `emptyForm` and the re-seed on an already-open form) — plus a per-row "Runs the pipeline: Yes" assertion on the Masters tab, scoped to the plant table because three masters share that screen and a page-wide count of "Yes" would pass on somebody else's cell. **484 unit + 46 e2e green, `vite build` clean.** The e2e suite ran for real here against `/opt/pw-browsers/chromium`, which the 2026-08-19 entry below could not do — it recorded 7/7 red at the sign-in screen, and that is no longer the state. All dozen doc sites corrected — including a comment in `supabase-setup.sql`, comment-only, no statement touched — ADR-0005's consequences amended and **no new ADR written** — activating a plant is the one-line switch ADR-0004 already promised, not a fresh decision. NOT done, deliberately and per the ticket: no logins or SQL (so no password was chosen or handled), no `serves` edits, no Free Stock split by plant, no opening stock, no migration. | Two. (1) **A flag can land completely and change nothing anybody can see — and that is not a bug, it is a thing to say out loud.** An admin already sees every tab regardless of plant and no plant logins exist, so `manufactures: true` on these two grants nobody anything today; it starts working the day their credentials are created. The single observable result is four entries in one dropdown instead of two. The temptation is to report "Lepakshi and Tapi are live for their teams", which is false in a way that only surfaces weeks later when someone asks why the team never signed in. **State the observable, and name the switch that is still off.** (2) **When a flag is documented as "the one-line change", check which direction that line runs.** ADR-0004 promised reclassifying a plant costs one boolean, and it was right — *outbound*. Removing a plant from the shop floor really is one line, because an intersection fails on either input. Adding one needs both. A promise about a symmetric-sounding operation on an asymmetric mechanism is the kind of true-but-halved statement that reads as a complete answer, and #155's own trap is its sibling: an example chosen because it is currently false is a hostage to whoever makes it true.

2026-08-27 | `src/lib/calc.test.js` + `e2e/roles.spec.js` — ticket #155, the "cannot register coils" fixture | Every test that needed *an example of a plant that cannot register a mother coil* reached for **Lepakshi** — a real plant, and one about to be activated. The suite would have stayed green while asserting the opposite of the truth: flip `manufactures` in `src/data/plants.js` and both of `accessFor`'s refusal branches lose their example at once (`manufactures: false`, and *on the master but off `COIL_INWARD_PLANT_IDS`*). The rollout-list test was the sharpest case — it built its constructed master by turning Lepakshi's own flag **on**, so activation would have made its premise and its subject the same thing. Third occurrence of one trap: the 2026-08-18 entry below records it as KERALA in the state→region seed, and repointing that at ODISHA only bought time, because ODISHA is a real state too. | Two invented plants — `fictional-never-manufactures` and `fictional-never-rolled-out` — on `MASTER_WITH_FICTIONAL_PLANTS = [...PLANTS, …]`, each carrying the full `src/data/plants.js` row shape. The ids state the invariant and the comment enforces it: nothing may ever add them to the plant master or the rollout list. **Four non-vacuity assertions** added (`plantById(id, master)?.manufactures` is `false` / `true`, and `COIL_INWARD_PLANT_IDS` does not contain the id). The e2e case moved to **Unattributed** — the one header scope no rollout can activate, because it is the labelling gap, not a plant. No production code, no assertion deleted or loosened; 484 unit + 44 e2e green. Verified by replaying the activation (`manufactures: true` plus the id on the rollout list): **2 of the old tests fail, 0 of the new ones**, and deleting either gate in `accessFor` kills exactly its own test. | **A fictional example is not enough on its own — it has to be fictional AND present.** Swapping a real example for an invented one moves the test onto a second, quieter failure mode: an id matching no master row takes the *unknown-id* branch, which hides the very same tabs, so the test passes for a reason that has nothing to do with what it claims to prove. The premise has to be asserted next to the conclusion — a test that says WHY it expects what it expects cannot rot into a coincidence. Second lesson, on the recurrence: ODISHA-for-KERALA was a like-for-like swap, and that is exactly why the trap came back a third time. The replacement has to differ in KIND — invented, not merely unmapped-so-far — or you have not removed the hostage, only chosen a new one.

2026-08-21 | `src/App.jsx` + the four docs — review round on the plant-scope fix above | A two-axis review (Standards / Spec) of the entry below found six real things the fix itself had missed, and they fell into two shapes. **Shape one, the fix reproducing its own bug one stage over:** Coil Inward got a scope-seeded plant default precisely so a new coil would not vanish from the register it was added to — and Production, saving a batch with NO allocations, derives `plant` from those allocations, gets `''`, and the row vanished the instant it was written, at the one stage with no plant field to correct it with. Unallocated is a *supported* flow ("saved unallocated until a coil is slit"). It had been broken for plant users all along; scoping admins too made it visible. **Shape two, docs that were only half-corrected:** the fix rewrote `ARCHITECTURE.md` and `DATA-MODEL.md` and left `UI-PATTERNS.md` still asserting "`viewPlant` — null for an admin" — the file `CLAUDE.md` routes all stage-form work to, so the next agent reads the false statement first. `ARCHITECTURE.md` also kept a parenthetical ("the tabs never filter themselves") that the paragraph beneath it now contradicted, `TESTING.md` still counted 31 tests, and a `calc.js` header comment still listed the old five-tab scope. | Production falls back to `targetPlant` when and only when there are zero allocations and the scope is a real plant — one allocation and the strip decides, as always. Coil Inward's default became three-branch: All Plants → `DEFAULT_COIL_PLANT` (the ordinary path stays one click), Hyderabad/NPMD → that plant, Lepakshi/Tapi/Unattributed → **blank**, because those cannot register coils and guessing Hyderabad recreated the very vanishing row the default exists to prevent; plus a `useEffect` so moving the selector re-seeds a form already open. `plantFileSuffix` gives all three stage CSVs the `-npmd` name suffix the Reports workbooks have had since #117 — bare under All Plants. The `const viewPlant = selectedPlant` alias was deleted (a second name is a second thing to keep in sync; the PROP keeps the name, where it earns it). All four docs corrected. 5 more e2e tests, 4 watched failing against the pre-review code — the 1 that passed is the unscoped-CSV control. NOT done: the reviewer's "duplicated `filterByPlant` call sites" and "e2e locator style" findings were declined with evidence — see the insight. | Three. (1) **A fix that introduces a mitigation should be run against every sibling of the thing it mitigates.** "A row that vanishes the moment it is written" was identified, named in a comment, and fixed on Stage 1 while Stage 3 had the identical failure in the same commit. The mitigation itself was the tell: writing the sentence should have triggered a search for every other place it was true. (2) **Half-corrected docs are worse than uncorrected ones.** Two files said the new truth and two said the old, so the repo now *disagreed with itself*, and `CLAUDE.md`'s routing table sends the next reader to one of the stale ones first. The rule that follows: when a change falsifies a documented claim, grep the claim, not the file you happened to be editing. (3) **Verify a review's findings before acting on them.** Two of ten did not survive: the "duplicated `filterByPlant`" is two call sites of one shared helper — which is what a helper is *for* — and the "wrong e2e locator style" is the exact idiom `e2e/pipeline.spec.js:19` already defines as `selectFor`. Both would have been real churn justified by an authoritative-sounding report. A reviewer's confidence is not evidence; the file is.

2026-08-21 | `src/App.jsx` — the plant selector stopped at the three screens where plant is actually recorded | Reported from the floor: with **NPMD** picked in the header, Coil Inward, Slitting and Production still listed Hyderabad's coils. The cause was one line — `const viewPlant = plantPinned ? selectedPlant : null` — which handed the stages a scope **only when no selector existed**. So a plant user was scoped and an admin never was: picking NPMD scoped Dashboard, Coil Tracker, Dispatch, Orders, Sales and Reports and left the pipeline unscoped, the header contradicting the table beneath it. Auditing the rest of the scope turned up three more, two of which reached a **plant user** as well, who has no selector to blame: (a) Slitting's `filteredBabyCoils` applied `viewPlant` *inside* the relative-cutoff branch, so **"All Time" — the default the tab opens on** — and a custom range both returned every plant's baby coils; (b) Production's CSV exported the raw `productions`, so a scoped screen produced an unscoped file naming no scope anywhere in it (Coil Inward and Slitting already exported what they showed); (c) every call site tested the scope for **truthiness**, and `''` (Unattributed) is both a real selection and falsy — selecting it widened back to every plant. | `viewPlant = selectedPlant` for everyone, passed as the `ALL_PLANTS` **sentinel** rather than a nullable id, so `filterByPlant` does the deciding and (c) cannot recur. Slitting applies the scope once, above the date branch. Production's CSV and both CSV `disabled` guards read the shown arrays. Coil Inward's new-coil plant now *defaults* to the scoped plant (a default, not a pin — the picker stays live for an admin), because a coil saved under NPMD's register defaulting to Hyderabad vanishes the instant it is written. 7 e2e tests in `roles.spec.js`, each watched failing against the original code: 6 red, and the 1 that stayed green is the "All Plants changes nothing" control. 479 unit tests and the 25 existing e2e still pass; `vite build` clean. Docs corrected in `ARCHITECTURE.md` and `DATA-MODEL.md`, both of which asserted the old behaviour as deliberate. NOT done: Masters stays unscoped (company-wide masters, not rows sitting at a plant), and `operatingPlant` still outranks `viewPlant` for Production's allocation pickers — editing another plant's batch must not hide the coils it already consumed. | Three. (1) **A scope with two derivations has two behaviours, and the tested one hides the other.** `plantPinned ? selectedPlant : null` reads like a guard and is really a branch: the plant-user arm had e2e tests and worked, the admin arm was a silent `null`. Collapsing it to `viewPlant = selectedPlant` did not just fix the bug, it removed the second arm that could rot. **One control must mean one thing; a scope that varies by *who is asking* is two features wearing one name.** (2) **A default that skips the scope is a leak on the default path.** Slitting filtered on three of five date options — and the two it skipped were "All Time" (what everyone sees on load) and "custom". Placing a filter inside a branch makes it a property of that branch; it belonged to the screen. Ask of every filter: *which code path does NOT run this?* (3) **The export is the table, downloaded.** Two of three stages already knew that; the third read the raw store, so the same screen answered one way and its download another. A scoped view and an unscoped export from one button is worse than either alone, because the file carries the screen's authority and none of its caveats — the same lesson `opts.fileSuffix` learned for Reports, unlearned one tab over.

2026-08-20 | `replaceAllRows` in src/lib/db.js — the delete-before-insert that turned a rejected write into an empty order book | Follow-up to the entry below, which fixed the missing column and left the mechanism that made it catastrophic. `replaceAllRows` superseded FIRST and inserted SECOND, in two separate HTTP round-trips with no transaction spanning them. So the class of failure was never "the plant column is missing" — it was **any** rejected insert (a new column, one bad row, a dropped connection, a expired token mid-upload) wiping the table it was rebuilding. `orders` is hard-superseded and lost the rows outright; `dispatches` is soft-superseded and would have hidden them. The tempting fix — one Postgres function doing both inside a transaction — is the wrong one HERE, and for a pointed reason: it is DDL, and un-run DDL is precisely what emptied the book. A fix that only works once somebody remembers to run SQL does not fix this bug. | Inverted the order: (1) read the live ids server-side, (2) insert the rebuilt set, rolling the inserted chunks back if any chunk is rejected, (3) only then supersede exactly the ids from step 1. Pure app-side, no schema change. Step 3 can no longer use a predicate (`neq('id', <impossible>)` / `eq('deleted', false)` would both match the rows just inserted), so it is scoped to the id list — chunked at a new `ID_FILTER_CHUNK` of **100**, not the body's 200, because `.in('id', […])` rides in the query string where 200 UUIDs is ~7.8 KB against a ~8 KB request-line limit. A supersede failure deliberately does NOT roll the new rows back. `emitSyncError` gained a `recovery` sentence and `SyncErrorBanner` shows it instead of its generic "changes will NOT persist" tail. Nine tests, each watched failing against a deliberately reintroduced defect: swapping steps 2 and 3 reds three of them, deleting the rollback reds one, setting `ID_FILTER_CHUNK` back to 200 reds one. Docs in `docs/DATA-MODEL.md`. NOT done: the two steps are still not atomic, and the unchunked `.delete().in('id', legacyDeleted)` in `pull()` (coils/baby_coils legacy purge) has the same URL-length exposure and was left alone as out of scope. | Two. (1) **Pick the invariant, then let it decide every failure path.** The invariant here is *a failed upload never leaves fewer rows than it started with — worst case duplicates, and a re-upload heals them*. Stated, it settles arguments that otherwise go in circles: should a failed supersede roll the new rows back? No — that trades the healable failure for the unhealable one. Should a failed rollback throw? No — it would bury the error that says why. Both answers fall out of the invariant rather than out of taste, and "duplicates are recoverable, loss is not" is the asymmetry doing the work. (2) **The July fix and the August fix look like opposites and are not.** July said "never supersede from a list of ids"; August needs a list of ids. The reconciliation is that July's real defect was the list coming from a **page-load snapshot**, not that it was a list — a server-side read issued inside the write has none of that staleness. A rule written down as its remedy ("use a predicate") rather than its cause ("don't trust stale local state") is a rule that will eventually block its own successor, and this one nearly did.

2026-08-20 | Supabase schema vs. the live database — the Sales upload rejected for a column that only existed in a file | "Upload failed: Could not find the 'plant' column of 'orders' in the schema cache", and the banner beneath it: `insert rejected for 200 rows`. Ticket #118 gave every order line a `plant`, shipped it in the app, and put `alter table orders add column if not exists plant text;` in `supabase-setup.sql` — a file **nobody runs against a live database**, because it also carries `delete from tubes;` and re-creates every policy. So the column existed in the repo and in the docs and not in the database. A column audit of every table the app writes found `state_regions` (the #101–#105 State → Region master) missing outright for the same reason; `coils.plant`, `baby_coils.plant`, `productions.plant`, `orders.ship_to_state`, `orders.distributor_code` and `app_credentials.plant`/`role` were all present. The damage was not the failed upload. `orders` is a **hard** replace-all (`REPLACE_MODE` in `src/lib/db.js`): `replaceAllRows` deletes every live row and *then* inserts, so the rejection left the order book **empty** — the "0 order line(s) · 0 open" on screen. | Applied `alter table orders add column if not exists plant text;` and the `state_regions` create + RLS policy to the live database, verified both present, and issued `notify pgrst, 'reload schema'` so PostgREST re-reads the schema the error names. Captured as **`migrate-orders-plant.sql`** at the repo root — the same pattern as `migrate-login-plant-role.sql`: those statements extracted **verbatim** from `supabase-setup.sql` and nothing else, guarded so it is safe to re-run (proved by running the file's own body against the live database a second time), with the "re-upload the Sales Excel to rebuild the book" step written into the header. No app code changed: nothing in `db.js` or `App.jsx` was wrong. NOT done — the delete-before-insert fragility below is left standing, and no PostgREST-level smoke test was possible (this environment's network policy blocks the project's REST host, so verification is SQL-level only; the real proof is the operator's next upload). | Three. (1) **DDL in a file that nobody dares run is not a migration.** `supabase-setup.sql` is a *bootstrap* file; the moment it also carries `delete from …` it stops being safe to re-run, and every `add column if not exists` inside it becomes a statement that will never reach a live database. Ticket #118 passed its tests, passed review and shipped — because every test runs against a schema built from that file, so the tests and the repo agreed with each other and neither had ever met production. **A schema change needs its own extracted, guarded, re-runnable file, or it has not shipped.** Two tickets have now landed this way (#125/#126 locked users out; #118 emptied the order book); that is a pattern, not an accident. (2) **The blast radius came from the replace order, not the missing column.** `replaceAllRows` supersedes server-side *before* it inserts — deliberately, to kill the July 2026 double-count — but the delete and the insert are two separate round-trips with no transaction between them, so **any** rejected insert (a new column, a bad row, a dropped connection) empties the table rather than leaving the previous day's book intact. `dispatches` is soft-superseded and survives this; `orders` is hard and does not. The safe shape is stage-then-swap, or one RPC that does both inside a transaction. (3) **"The schema cache" in a PostgREST error is a real component, not a figure of speech** — it is worth reloading explicitly so nobody concludes the SQL failed when it was only unread.

2026-08-20 | App login gate — the live database was two tickets behind the app | Asked who the app's users were, the answer was one row: `admin`, on a July 2026 `app_credentials` of `(id, login_id, password_hash, updated_at)`. No `plant`, no `role`, and **no `verify_login_details`** — tickets #125/#126 shipped in `src/App.jsx` and `src/lib/db.js` and were never run against Supabase. That is not drift, it is an outage: `verifyLoginDetails` is the ONLY sign-in path `LoginGate` has (`verifyLogin` is still exported at `db.js:305` but called from nowhere), so the RPC errored, the `catch` at `App.jsx:3627` fired, and every fresh sign-in read **"Could not reach the server."** Nobody had reported it, because a valid `jsw:auth` session lasts ~30 days — the only people who could still get in were the only people who would have noticed. | `supabase-setup.sql` already carried the fix but is written for a **new** database and also runs `delete from tubes;` and re-creates every policy. So its "APP LOGIN GATE" section was extracted **verbatim** (lines 418–441, 447–460) into `migrate-login-plant-role.sql` at the repo root — the `restore-baby-coils.sql` precedent — and run. Preconditions were checked read-only first (`crypt` present, 0 columns, 0 constraint, 0 function, 1 row), so every guarded branch was known to fire exactly once. `admin` backfilled to `role='admin'`, `plant` NULL. Then `hyderabad` and `npmd` were created, and the sign-in path proved end-to-end: correct password → right plant + role, wrong password → no rows. `blueprints/manage-app-login.md` gained a "bring an EXISTING database up to date" section with a two-count era check. | **Shipping the app does not migrate the database, and nothing tells you.** The repo's own history hides this: every schema change lives in one file called *setup*, which reads as something you run once, so a merged PR feels like a completed change when half of it never left the repo. Worse, this failure mode is **silent by construction** — a session-based login means the failure is invisible to exactly the people with the power to report it, and it disguises itself as a network error, sending the next person to the env vars. Two rules follow. (1) A ticket that adds SQL is not done at merge; it is done when a query against the live database says so — which is why the blueprint's new section leads with a `select`, not a paragraph. (2) When an app can only fail for users who are not currently looking, **the check has to be scheduled, not triggered by a complaint.**

2026-08-19 | Daily messages + scripts — ticket #128, the per-plant split on the phone (#117 phase 4, closing) | The workbook gained the plant split in #127; the two daily WhatsApp messages had not. That is the **same gap #101–#105 left for regions** and the 2026-08-18 entry below recorded: a dimension lands in the artefact with tests and stops at the artefact people actually read every morning. The second half was subtler — `scripts/servable-orders.mjs` advises what "the plant" can serve from stock on hand, and with four plants attributed an unnamed floor is no longer a harmless shorthand but a claim. | `scripts/region-mtd.mjs` is now `scripts/daily-splits.mjs` and emits **both** cuts — `{ date, month, regionSplit, plantSplit, rows }` — off one fetch and one `D`. The plant half is `buildPlantMtdSummary`, the identical function the workbook's BY PLANT block renders, so there is one implementation and nothing to disagree with; the message's Invoiced label comes from `invoicing.suffix` / `.note`, derived, so the day NPMD invoices every message changes by itself. The script exits non-zero if **either** split misses its tie-out. `servable-orders.mjs` names its floor from the production rows' own `plant` via a new `plantNamesIn(rows)` (master order, `Unattributed` last), says `Hyderabad + NPMD — both floors, combined` with a ⚠️ footer when stock spans two, and the `--agg` bundle now groups production Σs per SKU **and** per plant so the offline path can still answer. | Three lessons. (1) **A number is only "the same number" if it is the same code** — plant *is* a column, so a `GROUP BY` in the report SQL would have added up and been wrong in the way ADR-0003 describes: not arithmetically, but as a second implementation of a figure that already existed. (2) **The scripts were the untested half of the daily reports.** `scripts/daily-messages.test.mjs` now spawns them as real Node processes against `--in` fixtures and asserts the emitted plant split equals `buildPlantMtdSummary` field for field — proved by breaking the script and watching three tests go red. It doubles as the module-resolution guard for the scripts themselves. (3) **Naming is a feature when the count changes from one to four.** "The plant" was accurate while there was one; #128 does not scope the servable report per plant (what `--serves` means for NPMD is a business question #117 left open) but it stops the message implying a single floor, and states the combined-on-hand limit where the figures are rather than in a doc nobody has open at 8am.

2026-08-19 | reports.js — ticket #127, the per-plant split in the PB MTD workbook (#117 phase 4) | The workbook had to gain a per-plant breakdown **without one headline number moving**, and the temptation was to reuse what phase 1 already shipped: the plant filter. Filtering the workbook per plant would have produced the split as a by-product — and would have been wrong in the one way that matters. Scoping the report to Hyderabad drops Pending to Dispatch from 2615.441 MT to 761.441 MT, which on a Monday morning reads as a broken system, not as an attribution fix. **A filter and a breakdown answer different questions**: a filter says "show me less", a breakdown says "the same total, sorted". | `buildPlantMtdSummary` in `reports.js`, reached as `buildMtdDashboardData(...).plantSplit` so the workbook and any future reader share one object. It computes NO new arithmetic: each row is `salesKpis(filterByPlant(orders, p), filterDispatchesByPlant(upToD, p), MONTH)` — the very composition the header selector uses — so a plant's row and that plant selected in the header cannot disagree. Rendered as a `BY PLANT` block **beneath** the KPI cards on the Dashboard sheet, closed by an `ALL PLANTS` row, with `checks.invoicedTiesToAllPlants` / `pendingTiesToAllPlants` asserting the rows sum back to the same rows counted ungrouped (and printing a red "do not circulate" line on the sheet itself if they ever don't — a workbook that refuses to download tells its reader nothing). Two details that took thought: (1) **an unknown plant id must FOLD into the single `Unattributed` row**, not open a second row with the same label — two rows both reading "Unattributed" would add up and read wrong, so the group key is `plantById(key) ? key : ''`; (2) **the "Hyderabad only" label is derived, not written** — `invoicing.onlyPlant` comes from which rows carry invoiced tonnage, so it stamps itself onto the block header, the INVOICED MTD card caption and both distributor sheets' Invoiced columns, and every one of them changes by itself the day NPMD raises its first invoice. 14 new tests (89 in `reports.test.js`, 416 overall), `vite build` clean. | **Naming a mismatch can be the deliverable.** The reports have always compared four plants' Pending to Dispatch against one plant's Invoiced; the instinct is to fix the ratio, but nobody in the code has the standing to decide whether it should be per-plant — that belongs to whoever reads the report, once they can see the split. Labelling it and stopping was the correct scope. Second lesson, the one that generalises: **when new work must not move an existing number, the strongest test is not asserting the number — it is asserting that a change on the new axis leaves everything else byte-identical.** `expect(a).toEqual(b)` over the whole KPI object, the region blocks and the distributor × SKU rows, with every order line re-attributed to a different plant, catches an accidental coupling that a hand-written expectation for each figure would miss entirely.

2026-08-19 | db.js + supabase-setup.sql + CONTEXT.md — ticket #125 review follow-up | Two findings the local Postgres run could not have caught, both about MEANING rather than mechanics. (1) **Two sentinels, opposite meanings, and I picked the wrong one.** I returned `plant: ''` from `verifyLoginDetails` for the admin and wrote "'' = all plants" in three places. But this app already has a documented value for all plants — `ALL_PLANTS = '__all__'`, the pass-through branch of `filterByPlant` — and `''` is the OTHER plant sentinel: `Unattributed`, offered by `plantFilterOptions` as `{ id: '' }`. NULL on the credential column genuinely does mean all plants; the error was carrying that NULL straight out to a client value whose blank already meant something else. Handed to `filterByPlant`, the admin would have seen only the rows nobody could attribute — and nothing would have failed until the next ticket wired it up. (2) The **commented-out seed block** in `supabase-setup.sql` still read `insert into app_credentials (login_id, password_hash)`, which my own `role not null` had just made an error — the one instruction in the file a human runs by hand, broken by the ticket, and invisible to "ran the full file twice, clean" because a comment does not run. | Return `row.plant || ALL_PLANTS`; imported the constant from `calc.js` rather than restating `'__all__'`. New test asserts on what the value SELECTS (`filterByPlant(rows, admin.plant)` returns every row, `npmd.plant` returns one) instead of on the sentinel's spelling. Fixed the seed block, re-ran the cluster, and confirmed the finding was real by running the old form: `null value in column "role" … violates not-null`. Also: the backfill's `update … set role = 'admin'` has no `where` and the comment justified it by assuming only one login exists — replaced with what is actually true (before #125 the app had no roles, so every credential could already do everything; 'admin' describes it rather than promotes it) plus a blueprint step to demote a legacy extra login, tested. Added **Login role** to `CONTEXT.md`, un-staled **Operating plant**, and told `TESTING.md` that the database half is verified by `initdb`, not by vitest. 375 tests, build clean. | **When a value can be blank, find out what blank already means in this codebase before you assign it a meaning.** I invented "blank = all plants" inside a project that had spent four tickets establishing blank = Unattributed, and wrote it into the docs, which is how a private mistake becomes a shared one. The tell was there in my own diff: I had to explain the convention in a comment. A sentinel needing explanation is usually a sentinel that already exists somewhere else under a different name. Second: **the test that would have caught it asserts on consequence, not on value.** `expect(plant).toBe('')` passes for both readings; `expect(filterByPlant(rows, plant))` cannot. Third: a **commented-out** instruction is code that a human executes — my constraint broke it, and neither the SQL run nor the suite could see it, because the only thing that reads it is a person following the file's own advice.

2026-08-19 | supabase-setup.sql + db.js — ticket #125, sign-in returns a plant and a role | The whole ticket turns on one sentence — "running the SQL against a database serving the current build breaks nothing" — and that is a claim about a **database**, which the vitest suite cannot reach: the unit tests can only prove the client maps `{login_id, plant, role}` correctly, and would pass just as happily against SQL that errors on `alter table`, re-stamps a plant login as admin on a re-run, or leaves `verify_login` shadowed. Second trap, in the backfill: `update app_credentials set role = 'admin' where role is null` left standing looks idempotent and is not — the day someone adds a login and mistypes the role column, a re-run of the setup file silently promotes them. The #120 pipeline backfill had already solved exactly this and the answer was three lines up in the same file. | Wrote the tests first against an injectable client (the `replaceAllRows` seam), then **ran the SQL for real**: `initdb` a throwaway Postgres 16 in `/var/tmp`, rebuild the pre-#125 world from `git show HEAD:supabase-setup.sql`, seed an `admin` with a local throwaway password, and apply the new section to it. That proved what unit tests structurally cannot: `verify_login` still answers t/f afterwards, the admin kept its password and gained `role='admin'` with `plant` NULL, `verify_login_details` returns no rows for a wrong password, two further runs of the section left the `hyderabad`/`npmd` logins as `plant`, the check constraint and `not null` reject `'superuser'` and a missing role, and as `anon` the function executes while `select … from app_credentials` is still permission-denied. Then the full file on a fresh database, twice, clean. Backfill gated on the column not existing, per #120. Cluster deleted; no password reached any file (grepped). 374 tests, build clean. | **When the acceptance criterion is about the migration rather than the code, the test has to be the migration.** Everything I could assert in vitest was true of a version of this SQL that would have taken the live app down, because the client half cannot observe whether `verify_login` survived — so the only honest evidence was a real cluster, and it cost about ten minutes. Reach for `initdb` whenever a ticket's risk lives in DDL. Second: **"additive" is a property of the whole file, not of the new lines.** The new function is obviously additive; the re-runnability of the backfill beside it is where the damage would have been, and it is only visible on the *second* run. Third: the idempotency pattern to copy was already in this file — checking whether the last person to solve this problem left the answer behind beat inventing one.

2026-08-19 | App.jsx `Production` + CONTEXT.md — ticket #124 review follow-up, the six judgement calls | Taken after the three hard findings, and two of the six turned out to be worth more than tidiness. **"One rule, two mechanisms"**: the FIFO path expressed the plant scope by carrying `plant` onto adapter rows, the dropdown by calling `filterByPlant`, and my own save-guard fix had just added a third expression of it — three correct copies of one rule, which is the exact shape the #121 `withDispatchEntries` entry below exists to warn about. **"Mysterious Name"**: the prop was `selectedPlant`, but inside Production it never means "which plant am I looking at" — it means "which plant am I working as", and those diverge the moment you edit another plant's record. The name was importing the header's meaning into a component that had a different one. | Hoisted `babyCoilsAtPlant = filterByPlant(babyCoils, targetPlant)` and pointed the FIFO adapter and the dropdown at it — one statement of the scope, two readers. The save guard deliberately does **not** read it and re-scopes from the raw array: a guard whose job is "do not assume the scoping happened" must not be handed a list that assumes it. Renamed the prop to `operatingPlant` and added **Operating plant** to `CONTEXT.md` as a defined term against **Plant inheritance** — the plant you work *as* vs the plant a saved row *has*; phase 3 then swaps the source to the login with nothing inside Production changing. Also: merged the two `<Section>` branches into one with a conditional body (the form is withheld, not replaced by a different screen); made the `aria-label="Plant"` load-bearing by locating the e2e helper with `getByLabel` instead of `header select`; logged the field change in `blueprints/add-new-stage-field.md` with the "budget for both checks" lesson; and stopped the copy reading "from Unattributed only" as though it were a fifth plant — `scopeName` says "coils with no plant recorded" instead. 369 tests pass, build clean, and the composition re-verified end-to-end (dropdown, FIFO and guard) after the refactor. | **A prop name that is right at the call site can be wrong inside the callee.** `selectedPlant` is exactly right in `InventoryApp`, where it is a view filter, and wrong in `Production`, where it answers a different question — and the rename is what made the phase-3 migration self-evident rather than something to remember. Rename at the boundary; a component's props are its own vocabulary, not the parent's. Second: **"correct duplication" is the dangerous kind.** All three expressions of the plant scope were right, so nothing failed and no test caught it; the cost is entirely in the future, when one is edited. That is why it is worth taking a judgement-call finding seriously even when the code demonstrably works — the review is the only signal you get, because the code will not produce one until it is too late. Third: the guard's *exemption* from the hoist is as important as the hoist. Sharing one derived value is good until the consumer's whole purpose is to distrust that derivation.

2026-08-19 | calc.js + App.jsx `Production` — ticket #124 review follow-up, the hard findings | Two-axis review found three hard breaches, and the worst was one **both** axes reached independently: the docs I had just written asserted "a cross-plant batch is no longer reachable through the form", and it was reachable. Scoping the two pickers constrains the **list**, which is recomputed from `targetPlant` on every render and is therefore always right; the operator's rows are `manualAlloc`, which is **not** recomputed from anything and survives a plant change. So: pick Hyderabad's coils → switch the header to NPMD → add a row → save. Both pickers were correctly scoped at every single moment, `enriched` resolved the stale row against the raw `babyCoils` array, and `save()` persisted an allocation spanning two plants — filed by `productionPlant` as Unattributed, which made it look like the legacy-data case the docs said it could only be. I had reasoned about the filter and not about the state the filter does not own. | Added `crossPlantAllocationRows(rows, babyCoils, plant)` to `calc.js` — deliberately **not** in `App.jsx`, per the `plantForErpRow` lesson below, so the suite can reach the invariant — and blocked `canSave` on it with a red badge naming the offending coils. It returns the rows rather than a boolean so the badge can name them, and drops nothing: silently deleting an operator's row loses tonnage they entered, which is worse than refusing the save. Reproduced the exact reported sequence against the real helpers before and after (pre-guard: stored `plant: ''`; post-guard: save disabled), then 8 unit tests, 3 verified load-bearing by unscoping the helper. Also fixed the two doc breaches: `CONTEXT.md`'s Plant selector entry still read "does not scope … Production" and gained a new **Operating plant** term (the plant you work *as*, vs Plant inheritance, the plant a saved row *has*); and `docs/ALGORITHMS.md`'s diagram claimed plant is filtered before width, when width and `consumed` are applied by Production's adapter before `coilFifoAllocate` is called at all — redrawn as the two hops it really is. 369 tests pass, `vite build` clean. | **A filter over a derived view does not constrain state that outlives the view.** The picker list is derived, so scoping it is total; `manualAlloc` is retained state, so scoping the list says nothing about what it holds. Any time a guard protects a rendered list while a separate, longer-lived variable feeds the write, the invariant has to be re-asserted **where that state is consumed**, not only where the view is built — and the giveaway is that the two are recomputed on different schedules. Second: **the review caught this, and my own "verify the fix is load-bearing" step did not** — I reverted the one-line filter and watched 6 tests go red, which proved the filter worked, and proved nothing about the code path that never consulted it. A test that confirms a mechanism fires is not a test that the invariant holds. Third, on documentation: the DATA-MODEL sentence claiming unreachability is what made the gap *findable* — an overstated doc is more useful than a vague one, because a reviewer can falsify it. Weaken the claim only after the code earns it, never to make the sentence true.

2026-08-19 | calc.js + App.jsx `Production` — ticket #124, production and FIFO never cross plants | The ticket's own AC contains a circularity worth naming before writing code: the pickers must scope to "the production's plant", but a production's plant is *derived from the coils it consumes* (`productionPlant`, #120) and must never be typed on the form. So the scope cannot come from the record being composed — it has to come from outside it, and before phase 3's per-plant logins the only "which plant am I" signal in the app is the header selector (#121). That selector is deliberately **not persisted**, which makes it a poor stand-in (every reload drops back to All Plants), and its documented job was "scope these read views", not "identify the operator". Second trap, found by re-reading my own first draft rather than by a test: I wrote `editing ? (record.plant \|\| selectedPlant) : selectedPlant` and gated only `!editing && selectedPlant === ALL_PLANTS` — so **editing** a record storing blank `plant` (an unallocated batch, or a legacy row) fell through to `ALL_PLANTS` with the gate switched off, and the pickers spanned every plant. The exact leak the ticket exists to close, reachable in the one code path I had excluded from the guard. | `coilFifoAllocate` gained a `plant` argument defaulting to `ALL_PLANTS` and applies `filterByPlant(coils, plant)` **in place of** its `(coils \|\| [])` opener — so the filter is structurally ahead of width/thickness/consumed/capacity rather than merely earlier in a chain of `.filter`s. Production passes `targetPlant = editingProduction?.plant \|\| selectedPlant` (collapsing both branches, so `needsPlantChoice = targetPlant === ALL_PLANTS` guards *every* path), carries `plant` through the `babyAsCoils` adapter, and wraps the manual dropdown in the same `filterByPlant`. Under All Plants the form body is replaced by an amber "choose a plant in the header selector first" rather than defaulting to Hyderabad. 9 cases added to the **existing** `coilFifoAllocate` describe block per the AC (361 total, 6 of the 9 verified load-bearing by reverting the one-line filter and watching exactly those fail); `vite build` clean. `save()`, `productionPlant`, the off-spec override, the ✓ flag, the 0.02 MT threshold and the never-auto-save rule are all untouched. Re-ran e2e against the throwaway Chromium-1194 config: still 7/7 red on the same `Sign in to continue` screen on clean `main` as on this branch — so I dropped the two browser tests I had drafted (they used `selectFor(…, 'SKU')`, which targets a `<select>` while Production's SKU field is a `SearchSelect` — an unverifiable test is worse than none) and kept only the four one-line `selectPlant(page, 'Hyderabad')` preconditions the new gate genuinely requires. | **When a rule must apply "before" others, express it as the collection the others read, not as an earlier condition.** Putting `filterByPlant` where `(coils \|\| [])` used to sit makes "another plant's coil does not exist here" unremovable by anyone appending a rule later; an extra `&& samePlant(c)` inside the existing `.filter` would have read as one eligibility test among peers and invited exactly the reordering the ticket forbids. Second, and the one that nearly shipped a bug: **a guard written as "new records need X" is a guard with a hole, because the fallback that makes editing safe is itself conditional.** Collapsing to a single derived value and testing *that* value for the unsafe sentinel removed the branch instead of adding a second check to it. Third: the honest answer to "which plant" under All Plants is to ask. Defaulting to Hyderabad would have kept every existing test and e2e path green while showing an NPMD operator someone else's steel — the cheapest possible fix and the only one that recreates the mis-attribution #117 exists to end.

2026-08-19 | calc.js — ticket #123, NPMD registers and slits its own coils | #123 is blocked-by #122, and #122's own commit message said "this ticket only lands the tested numbering underneath it" — `COIL_INWARD_PLANT_IDS` was left as the one remaining gate. Before touching anything, grepped `App.jsx`/`calc.js` for every hardcoded `'hyderabad'` string to confirm that claim rather than assume it: exactly one hit, the gate itself. Slitting's `save()` already derives baby-coil plant from the mother via `babyCoilPlant` (no plant filtering anywhere in it), `CoilTracker` already receives the header selector's `filterByPlant`-scoped arrays, and `coils`/`baby_coils`.`plant` are plain `text` columns with no CHECK constraint — so every acceptance criterion downstream of "NPMD can register a coil" was already satisfied by #120/#121/#122 and needed no code. Also had to settle what "the existing Playwright specs pass unchanged" means given the 2026-08-18 entry below recorded them as 7/7 red since the login gate shipped in July — an assumption worth re-checking, not inheriting blind. | Widened `COIL_INWARD_PLANT_IDS` to `['hyderabad', 'npmd']` (one line) and updated the one test that pinned the old Hyderabad-only list (`calc.test.js`, plus strengthened its `manufactures`-toggle assertion to check both plants independently, not just one). Updated the five docs that said "Hyderabad alone until phase 2" (`DATA-MODEL.md`, `ARCHITECTURE.md`, `UI-PATTERNS.md`, `adr/0005`, `blueprints/add-new-stage-field.md`) so the next reader isn't told a gate exists that no longer does. 352 unit tests pass, `vite build` clean. Re-ran the e2e suite against a throwaway config pointed at the pre-installed Chromium 1194 binary (the technique the 2026-08-18 entry recorded, since `@playwright/test` 1.60 wants revision 1223): still 7/7 failed, and the captured page snapshot for every one of them shows the same `Sign in to continue` screen at the same `getByRole('button', { name: '1. Coil Inward' })` timeout — confirmed unrelated to this diff, since none of these specs get far enough to reach a stage tab, let alone Coil Inward's plant select. Config deleted after the run, nothing e2e-related committed. | Trust a well-scoped predecessor ticket's own account of what it left undone, but verify it with a grep, not a re-read of the diff — #122's commit message and `DATA-MODEL.md` both named `COIL_INWARD_PLANT_IDS` as the one line left, and they were right, which is a property of that ticket having been scoped honestly, not something to assume in general. Second: "the existing specs pass unchanged" on a suite already known red is a claim about the FAILURE being unchanged, not about green — the only way to know that is to still run it and read what actually stopped it (the page snapshot, not just the timeout message), because a ticket touching Coil Inward is exactly the kind of change that could have shipped a new, earlier failure (e.g. a broken default plant crashing the form) that would otherwise hide behind the same red X.

2026-08-19 | Orders + Dispatch + calc.js — closing out the #121 review | The last two review findings. (1) **Two per-tab Plant dropdowns survived the global selector.** #118/#119 gave Orders and Dispatch each their own Plant `filters` entry; #121 added the header control but left both in place, so the app shipped three plant filters. Orders' was worse than redundant — its options were a HARDCODED `[...PLANTS.map(p => p.name), UNATTRIBUTED_PLANT]`, so scoped to Hyderabad it still offered NPMD/Lepakshi/Tapi, every one of which could only ever return nothing. `CONTEXT.md` already listed "Plant filter (as a per-tab concept)" under _Avoid_ while two of them existed. (2) **The dispatch weight derivation existed twice**, in two files: `buildDispatchRecords` (App.jsx) and `filterDispatchesByPlant` (calc.js) each computed `Σ entry weight` and `vehicleWeight − that` with their own copy of the arithmetic. | Deleted both dropdowns — the Plant **column** stays on both tables, only the filter goes, and Orders keeps its other six chips (Type/Size/Status/Customer/Pending/Order month). That left `PLANTS` unused in App.jsx, so the import went too. Extracted `withDispatchEntries(record, entries)` into `calc.js` and pointed both call sites at it: it sets `bundleEntries`, `selectedBundles`, `theoreticalWeight` and `variance` together, naming the invariant that all three are **functions of the entries**. 3 new unit tests (344 total), including one asserting the filter path and a direct call produce identical weight and variance — the drift the duplication invited. Browser-verified: zero Plant chips on either tab, Plant column present on both, and one mixed 10 T invoice reading 7.0 + 3.0 stacked at All Plants and 7.0 scoped to Hyderabad. | **A global control is only global once the local ones it replaces are deleted.** Adding the header selector without removing the per-tab dropdowns produced the exact confusion the spec's "nobody has to reason about which views are scoped" was written to prevent, and a hardcoded options list made it visibly wrong (plants offered that cannot match a row). When a feature supersedes an older one, the older one is part of the diff. Second lesson, on the duplication: it was harmless the day it was written — both copies were correct — and that is precisely why it is worth extracting. The failure mode isn't today's wrong answer, it's next year's edit landing in one file and not the other, leaving one invoice weighing two different things depending on which code path last touched it. The helper's name states the invariant so the next person doesn't have to infer it.

2026-08-19 | Reports under the #121 plant filter | The review's remaining spec gap: #121 says "a plant selector **every shared view** follows", but Reports was left unscoped, so the header could read "Inventory Management — NPMD" above a **company-wide** PB MTD workbook. Deliberate at the time — #117 phase 4 owns the reports split and explicitly wants the headline total to STAY at 2615.441 MT ("the headline number does not drop 1854 MT overnight for no business reason") — but the result was the app contradicting itself on one screen, which is the mis-attribution this whole spec exists to end. | Scoped it, and made the scope impossible to miss, because a workbook is **the one scoped output that is read somewhere else** — mailed, broadcast on WhatsApp, opened next week by someone who never saw the header. Three channels, because each fails differently: an amber banner on the tab (stops the mistake before the click), `— <Plant> only` in **every sheet's title** (one `opts.companyName` string already reaches all 7 title rows across the 3 workbooks — no generator changed), and a `-<plant>` suffix on the **file name** via a new `opts.fileSuffix` (the only half that survives a rename, a mail client, or a download list). Unscoped output is byte-for-byte unchanged. `reports.test.js` gained two cases by extending the existing download-path stub to capture `a.download`: scoped → every sheet title contains "Hyderabad only" and the file is `PB-MTD-Dashboard-2026-07-15-hyderabad.xlsx`; unscoped → no "only" anywhere and the plain company file name. Browser-verified end to end: the real download came back `PB-MTD-Dashboard-2026-08-19-hyderabad.xlsx` with the banner shown, and the plain name with no banner at All Plants. **Phase 4 is still owed** — a company-wide headline that keeps its total and gains a per-plant split beneath, Invoiced labelled Hyderabad-only. | **When a filter's output leaves the screen, scoping the data is only half the job — the artefact has to carry its own scope.** Every other scoped view is read on the screen that scoped it, so a banner suffices; a workbook is read with no such context, and `PB-MTD-Dashboard-2026-08-19.xlsx` holding one plant's tonnage is indistinguishable from the company report by the only thing a recipient sees. Note the seam that made this a 4-line change: all three generators already funnelled a single `companyName` into `writeTitle` for every sheet, so one string stamped 7 titles. Worth looking for that kind of existing single-point-of-entry before threading a new parameter through — the file name needed one and got `fileSuffix`; the titles did not. Second point: "phase N owns that" is a reason to sequence work, never a reason to ship a screen that disagrees with itself.

2026-08-19 | Sales + Dispatch under the #121 plant filter | **Two-axis code review of the shipped #121 selector found a live data-loss path and a silently mixed-basis KPI — both from the same root cause: a filter was allowed to change what a WRITE and a RATIO mean, not just what is displayed.** (1) `Dispatch.softDelete` matches the RAW store by `id`, but the records it renders are plant-scoped shallow copies. Deleting while scoped to Hyderabad soft-deleted the **whole invoice**, including the NPMD lines the operator could not see and never intended to touch. (2) `SalesDashboard` received plant-scoped `orders`/`dispatches` but unfiltered `estimates` — a Best Estimate carries no plant, and `salesByDistributor` deliberately emits a row for every distributor holding one even with zero orders. So `beTotals.be` stayed company-wide while `kpis.mtdInvoice` went plant-scoped, and **% of BE / Gap to BE / the Plant BE achievement line quietly became one plant's actual over four plants' plan** — the exact four-against-one mismatch #117 exists to expose, reintroduced by the feature meant to reveal it. A third finding: `docs/DATA-MODEL.md` still claimed the 761.441/2615.441 MT split was "asserted against the real 18-Aug-2026 figures" after the LEARNINGS entry had already retracted that. | Neither is fixable by filtering harder — a per-plant Best Estimate is explicitly out of scope in #117, and `deleted` lives on the dispatch RECORD while plant lives on its entries, so there is no per-entry delete to fall back on. Both are therefore **withheld** under a filter and say why: `Dispatch` takes `plantScoped` and passes `onDelete={undefined}` (which drops DataTable's whole Actions column) beside an amber note; `SalesDashboard` takes `selectedPlant`, renders `—` with the reason in a `title` for % of BE and Gap to BE, and swaps the Plant BE achievement clause for "company-wide, not Hyderabad's". The **Best Estimate column itself stays and stays editable** — a company-wide plan is a correct company-wide number; only the comparison was ever wrong. The Sales CSV blanks the same two columns, because an exported mixed-basis figure outlives the screen that explained it. Docs corrected to say the tonnage split is unit-level only. Verified in Chromium against stubbed PostgREST reads (the #102 technique): All Plants reads "Plant BE 50.0 T · invoiced 20% of it" with Gap-to-BE total 40.0 and one Del button; Hyderabad reads the company-wide warning with Gap-to-BE "—" and zero Del buttons, while the Weight column correctly shows 7.0 T of the mixed 10 T invoice. 339 tests pass, `vite build` clean, no console errors. | **A filter is a way of LOOKING at data — the moment it changes what a write touches or what a ratio divides, it has stopped being a filter.** Both bugs passed the first review and the full unit suite because the filtering helpers themselves are correct in isolation; the defect only exists at the seam where scoped data meets an unscoped collaborator (`estimates`) or an unscoped write target (the raw store). Unit tests over pure helpers structurally cannot see that seam, which is the argument for the browser pass, not against the helpers. Durable rule for the next filter: enumerate every write and every DIVISION on the filtered screen, and for each ask "does this still mean the same thing?" — if not, disable it and say why, never let it silently return a different answer. Second lesson: retracting a claim in one file does not retract it everywhere. `add1ad8` corrected `LEARNINGS.md` and left the same claim standing in `DATA-MODEL.md`; grep the figure, not the file.

2026-08-19 | App-wide (header) — ticket #121, Phase 1 of the #117 multi-plant spec | Feature — a single plant selector in the header, scoping Dashboard / Coil Tracker / Dispatch / Orders / Sales at once, defaulting to All Plants. Built on #118 (order lines carry plant), #119 (invoice lines carry plant) and #120 (pipeline rows carry plant, backfilled to Hyderabad) — all three landed on this same stacked branch first. | Two new pure helpers in `calc.js`: `filterByPlant(rows, selected)` — pass-through on the `ALL_PLANTS` sentinel, else `rows.filter(r => storedPlant(r) === selected)`, for anything with a top-level `plant` (coils, baby coils, productions, orders); `filterDispatchesByPlant(dispatches, selected)` — filters each record's `bundleEntries` (plant lives per-entry, not on the record) and re-derives `theoreticalWeight` from the survivors, dropping a record left with none. `plantFilterOptions()` returns the fixed order the `<select>` always shows: All Plants, the four plants, Unattributed last. `InventoryApp` holds `selectedPlant` (plain `useState`, not persisted — a reload always comes back to All Plants) and filters ONCE, passing the scoped arrays into Dashboard/CoilTracker/Dispatch/Orders/SalesDashboard as ordinary props; none of those five components filter themselves. Coil Inward/Slitting/Production/SKU Master/Reports keep receiving the raw store arrays unchanged. The header's hardcoded "Inventory Management — Hyderabad" now reads the selection. | **Filtering at one seam (the parent) rather than inside each tab is what makes "nobody has to reason about which views are scoped" literally true** — the five components have zero new plant-aware code; they just render whatever array they're handed, same as always. The two traps found while wiring it in: (1) Orders' upload path (`replaceOrders`/`replaceDispatches`, and the `productions` fed into `buildDispatchRecords` for the invoice coil trace) must stay on the RAW unfiltered data — filtering it would have made an upload made while scoped to one plant silently lose every other plant's coil trace, a correctness bug invisible until someone happened to upload while filtered; (2) Sales' `estimates`/`stateRegions` must also stay raw, because Best Estimate and Region are keyed by distributor/state, not plant, and the acceptance criterion is explicitly that scoping the header leaves them untouched. Verified at the `calc.test.js` unit level against the real #117 figures — order rows constructed to the published per-plant tonnages: `salesKpis` on plant-filtered orders reproduces 761.441 MT (Hyderabad alone) from the same 2615.441 MT (+ an Unattributed row) All Plants total, and summing every plant option's `filterByPlant`/`filterDispatchesByPlant` output reproduces the unfiltered total exactly — the same "a labelling gap may never make weight vanish from a sum" invariant `Unmapped`/`Unattributed` already carry elsewhere. **The live check (Supabase MCP, project `hztblmccvvarmgxmunrp`) could NOT confirm the 2615.441 / 761.441 figures, and found two things that block it — both deployment state, not code:** (1) **`orders` has no `plant` column in production** — #118's `alter table orders add column if not exists plant text` in `supabase-setup.sql` has never been run there (`information_schema` lists `ship_to_state` but no `plant`), so until it is, every order line reads Unattributed and the header filter does nothing visible on Orders/Sales; (2) **`orders` is empty — 0 rows** (it is replace-all on upload and currently holds nothing), so there is no 18-Aug order book in the database to measure at all. The pipeline side is in better shape and DID verify: #120's backfill is live and complete — 294 coils / 2057 baby coils / 1276 productions, **all `hyderabad`, zero NULL** — so scoping Coil Tracker and the Dashboard to Hyderabad shows everything and to any other plant shows nothing, exactly as intended. **Running #118/#119's SQL and one daily upload is the prerequisite for this ticket to do anything on the Orders and Sales tabs.** 339 tests pass (6 new), `vite build` clean, and a Playwright smoke pass (dummy Supabase creds, `--mode test`) confirmed the selector renders with the right options/order, the header text updates per selection, and all ten tabs render with no console errors while scoped to Hyderabad.

2026-08-18 | Reports / Orders (lazy chunks) | `Report failed: Failed to fetch dynamically imported module: .../assets/reports-BjkJ5Jvy.js`. Not a report bug — the tab was opened before a deploy, so it asked for a chunk hash Vercel no longer serves, and the SPA rewrite answered the `.js` request with `index.html`. | Both on-demand imports now go through `loadChunk` (`src/lib/chunk.js`): reload the tab once (sessionStorage guard, 60 s window) so the new hashes load; a second failure, blocked storage or offline shows a plain "refresh the page" message instead of looping. `main.jsx` installs the same one-shot reload on Vite's `vite:preloadError`, and `vercel.json` no longer rewrites `/assets` to `index.html`, so a vanished chunk 404s honestly. | The user-visible error named the report, so it reads like a report failure — the giveaway is the hash in the URL. Any deploy while a tab sits open re-arms this for EVERY lazy import (xlsx upload included), which is why the fix is a shared wrapper, not a try/catch on the Reports button. Never auto-reload without a persisted guard: private browsing blocks sessionStorage, and a reload that cannot remember itself never stops.

2026-08-17 | Sales tab — State → Region master (ticket #102) | Feature — show each distributor's **State** and **Region** on the Sales tab, region editable in place. Region is a business concept the ERP never exports; state now arrives with the data (#101). | Keyed the master by **state, not distributor**: new store `jsw:stateRegions` → `state_regions` (`unique(state)`, which is also the upsert arbiter — `CONFLICT_TARGET` in `db.js`), six seed rows in `src/data/stateRegions.js` with **fixed literal uuids**. `calc.js` gained `REGIONS`/`UNMAPPED_REGION`/`normStateName`/`stateRegionIndex`/`regionForState`/`distributorStateIndex`/`distributorRegionResolver`; `salesByDistributor` takes `opts.stateRegions` and returns `state`/`states`/`multiState`/`region` per row. `App.jsx` gained `RegionCell` (blue `<select>`, commits on change, `stopPropagation` so editing doesn't open the SKU drill-down) + State/Region columns + State/All States/Region in the Sales CSV. 15 new unit tests (247 total), `vite build` clean, and a throwaway Playwright smoke test (stubbed PostgREST reads) confirmed the render, the `+1` multi-state marker, the totals row, and that the write lands as `POST state_regions?on_conflict=state`. | **The store's "table if non-empty, else fallback" pattern is not enough for a seeded master that is edited one row at a time.** Editing one state writes ONE row, so on the next load the table is non-empty and the fallback never fires — the other five seeded states would silently read `Unmapped`. Fix: `stateRegionIndex` **layers the seed under the stored rows** rather than choosing between them, which also makes "an edited region overrides the seed" fall out for free. Corollary: clearing a region must store `region: ''` (an explicit un-mapping) instead of soft-deleting the row, or the seed would spring back. Second lesson: a missing mapping is a **labelling** gap — `Unmapped` is not a fifth region and its tonnage stays in every total, which is asserted by summing the rows grouped by region back to `salesKpis`. Also found: the two existing e2e specs have been failing on `main` since the July 2026 login gate landed — they `goto('/')` and never seed `jsw:auth`, so they hit `LoginGate` and no tab buttons exist (verified in a clean `main` worktree; not fixed here, out of scope).

2026-07-28 | Dashboard (FG cards) + reports.js — **supersedes the 2026-07-16 entry below** | **Flooring each SKU at 0 and THEN summing deleted 36.183 T of genuinely-dispatched steel from every FG total.** Surfaced reconciling the plant team's 28-07 stock sheet (1,816.413 MT): the system was reporting two different numbers — the WhatsApp/MTD report said **1,857.6 T** (raw `Σ produced − Σ dispatched`) while the Dashboard FG card said **1,893.826 T**. The 2026-07-16 fix floored per-SKU negatives at each SUM site, which is right for a row (a SKU can't HOLD negative stock) but wrong for a total (the pipe physically left the plant). 8 canonical SKUs are net-negative — 50X50X2.9 (−12.002), 60X60X4 (−6.868), 38X38X3.2 (−4.800), 38X38X2.9 (−4.150), 38X38X4 (−4.013), 80X40X1.6 (−3.639), 50NBX4 (−0.637), 25NBX4 (−0.074) — five with **zero** recorded production against a real invoice. | New pure `unmatchedDispatch(pool)` (`calc.js`, next to `producedPool`) returns the magnitude + SKU count of all negative `availableWeight`, so one definition feeds all three sites. **App.jsx**: `skuTotals` keeps the floored `inventory`/`free` accumulators (the SKU table TOTAL row must tie to its visible rows) and adds `unmatched`/`overCommitted`/`negSkus`; `fgLeft = inventory − unmatched`, `freeFg = free − overCommitted`; a 4th **"Dispatched w/o Production"** card renders the gap; new module const `EPS_MT = 0.05` shared with the over-dispatch alert (was a bare `-0.05`). **reports.js**: `physicalInventory = onhandTot − unmatched` (`skuAgeing` drops negative-stock keys entirely, so the tonnage had to be recovered from `producedPool`); `otherLe2` re-based on `onhandTot`; ladder gained a `− Dispatched w/o recorded production` row. `buildFinishedStockData` now returns `{grand, unmatched, net}` — rows still list only stocked sizes, and the sheet prints GRAND TOTAL (listed) → less unmatched → NET FINISHED STOCK. | **A per-SKU floor is a DISPLAY rule, not an accounting rule — apply it to the row and subtract the dropped tonnage back out of the total, or you silently un-ship real steel.** The invariant to hold everywhere: `Σ floored on-hand − unmatched === Σ produced − Σ dispatched` (unit-tested). Corollary: the SKU table TOTAL row now deliberately differs from the FG card by exactly `unmatched` — they previously agreed only because the discrepancy was hidden, so the gap must be rendered, not reconciled away. The MTD KPI caption already claimed "produced − invoiced"; it was inaccurate until this change. Verified end-to-end against live Supabase (`hztblmccvvarmgxmunrp`) by porting `canonicalSkuKey` to SQL: canonical grouping returns net **1857.643** / floored **1893.826** / unmatched **36.183** / 8 negative SKUs / 94 groups — identical to a size-key grouping, and the FG card now matches `reports/PB-MTD-Update-2026-07-28.md`. 190 tests pass (6 new), `vite build` clean. Direct Supabase access from Node is blocked by the sandbox's egress allowlist — live checks have to go through the MCP SQL tool.

2026-07-27 | Orders & Invoice (Upload Sales Excel) | Bug — every upload raised **"Sync failed (SKU Master): upsert rejected for 1 row. duplicate key value violates unique constraint `skus_sku_code_key`"** while the banner still claimed "+1 new SKU(s)" | Reproduced by replaying `buildDispatchRecords` against the live `skus` table (273 rows) and the uploaded workbook: the rejected row was `1141-13068-10078401` (MS CHS … 15 NBx2x6000). That product exists **twice under two ids** — as `SKU-255` in `DEFAULT_SKUS` and as uuid `42fea9a1…` in Supabase (created by hand in SKU Master on 26-Jul, `weight_per_tube` 5.7 vs the catalog's 5.7116). Four compounding causes: (1) the self-heal pushed the catalog object **verbatim, keeping its `SKU-nnn` id**, so the same product could be inserted a second time; (2) its only duplicate guard was `byCode` built from the in-memory `skus`, which `useSupabaseStore` fetched **once on mount and never refetched** — a SKU added later (other tab/user, or a previously failed sync) was invisible; (3) `upsert(…, {onConflict:'id'})` cannot absorb a conflict on a *different* unique index, so Postgres rejected the row and **failed the whole batch**; (4) the optimistic row stayed in React state with no rollback, so the UI kept showing (and re-sending) data the DB never accepted. Contributing: `mapDispatchRow` matched invoice SKUs **by description only**, ignoring the `MM ID` column that all 400 rows populate (while `mapOrderRow` already read it). Fix: extracted `skuImportResolver(skus, catalog, makeId)` into `calc.js` — matches **MM ID → description → canonical key**, and `adopt` treats code **or** canonical identity **or** description as proof the product exists, returning the **live** row instead of inserting a twin; a genuinely new SKU is added as a copy with a **fresh `uid()`**. `db.js` gained `conflictTargetFor()` (`skus` → `sku_code`, everything else → `id`, safe because no FK references `skus.id`) and re-reads the table on a rejected write. 178 tests pass; `vite build` clean; replaying the exact error state now yields an insert with a generated id and 0 unresolved SKUs. | **A UNIQUE column that isn't the upsert arbiter is a landmine** — one rejected row takes the entire batch with it, so the visible symptom ("SKU Master sync failed") is far larger than the actual defect (one duplicate SKU). Two independent layers were needed: the client must not *create* the twin (identity-based guard) **and** the write must survive one anyway (`onConflict: 'sku_code'`), because the client's guard reads a snapshot that can always be stale — data is fetched on mount and this app stays open all day. Matching on the ERP code before the description removes the whole failure class at the source: description drift is what pushed lines into catalog self-heal in the first place. Also note the master still carries **15 duplicate-canonical-key pairs** (same pipe under two codes, e.g. `RHS-100x50x2.50` vs `1140-13075-10074986`) and rows whose `sku_code` is a full description string — a separate data-hygiene cleanup, not a code bug.

2026-07-05 | Dashboard SKU-wise Inventory | Change — the SKU table's **"Pending to Invoice"** column now matches the Dashboard/Sales **"Pending to Dispatch"** (logic + name) | The column computed `Σ max(0, ordered − invoiced)` over **open-status** order lines (per-line netting via `orderLineInvoiced`), so its total (475.0 T) never reconciled with the Dashboard **"Pending to Dispatch"** card (498.6 T = Confirmed + Non-confirmed). Rewrote the `skuInventoryRows` pending as `Σ (confirmed + nonConfirmed)` per SKU — the exact `salesKpis` arithmetic — with **no order-status filter** (cancellations are already netted inside `nonConfirmed`); renamed the row field `pendingToInvoice`→`pendingDispatch` and the column/CSV/notes to "Pending to Dispatch". Blank-`mmId` orders bucket into a synthetic **"(Unmapped)"** row (added to the codes union) so the table total ties out to the card. `totalOrders`/`invoicedVsOrders`/Reserved/Inventory/Free are untouched. | The two "pending" numbers were the reported confusion ("how come they're different") — they measured different things under near-identical names. Tie-out holds at **All Time**: the SKU column still follows the period picker (by order date) while the card is always all-time, so they only match at the default. A new calc test asserts `Σ rows.pendingDispatch === salesKpis().pending` (with an Unmapped-bucket case) so the reconciliation can't silently regress. `salesNum` is referenced from `skuInventoryRows` even though it's declared later in the module — safe because closures resolve at call time (runtime), never during module load. 122 tests pass; `vite build` clean. The live 498.6 T spot-check needs the deployed Supabase data (absent in a fresh clone).

2026-06-26 | Sales dashboard | Bug — one distributor (**V V N STEELS P LTD**) showed as **two rows** | `distributorSalesRows` grouped distributors by the **free-text name** keyed with only `.trim()` (no whitespace/case normalisation), so the same party spelled differently between the Orders sheet and the (older, stored) Invoice records formed two groups; the 2-word display code ("V V") made them look like a duplicate. Both ERP exports actually carry a clean **`Distributor Code`** (V V = `0015g00000nOmU9AAK`, 1:1 with name) and **every invoice line links to its order** (`sku_id`==orders `Sku ID`, 0 unresolved on the real files). Fix: resolve ONE identity per distributor via `resolveDistributorIdentity` — for a shipment, adopt its **linked order's** identity first (orderLineId→orderId→childOrderId), else its own `distributorCode` (captured inside `bundle_entries` JSONB), else `normDistributorName` (collapse internal whitespace + upper-case). Group by that key, display the real first-seen name; the dropdown value is now `r.id` (the identity), label is the short code. | The merge happens at **read time** from the order link, so it fixes existing stored data with **no migration/backfill**. Deliberately did **not** add a top-level `distributor_code` column to `orders`: a stray top-level key fails the whole Supabase upsert if the column is missing (rows silently vanish), and the order-link resolution makes it unnecessary — the dispatch code stays safely inside the `bundle_entries` JSONB. Verified on the real Orders+Invoice files: V V collapses to a single row (743 T orders + 361.5 T invoiced). 99 calc tests pass; `vite build` clean.

2026-06-26 | Sales / Dispatch + Orders importers | Root cause — **blank distributor** rows in the Sales dashboard | The dispatch/orders Excel importers matched only the headers `distributorname` / `customer` / `billtoname` (via `pick()` in `mapDispatchRow`/`mapOrderRow`). Any other header — "Party Name", "Customer Name" (normalises to `customername`, **not** `customer`), "Consignee", "Sold To", etc. — fell through to `''`, and `distributorSalesRows` buckets every blank `customer` under one `'—'` row. Fixed by extracting a shared module-level `DISTRIBUTOR_HEADER_ALIASES` list (more specific names first so they win over a bare `customer`), used by **both** importers, plus a dispatch-upload warning (`N line(s) with no distributor — check the column header`, shown red) so a header mismatch is visible instead of silent. | Header normalisation strips spaces/dots/underscores but does **not** collapse word boundaries — `customername ≠ customer`, so every multi-word variant needs its own alias. Broadening the importer only fixes **future** uploads; rows already saved blank stay under `'—'` until re-uploaded (and the importer skips already-imported invoice numbers, so a clean re-import means deleting the affected blank-customer dispatches first).

2026-06-26 | App-wide (cost) + Sales | Feature — removed cost fields; added short distributor codes | **Cost removal:** dropped the coil **Cost Price** (Coil Inward field/table/CSV + the Slitting width-proportional cost split — kept the *weight* split) and PO Master **Item Price / Item.CF.Updated Price**; gutted the coil-cost math from `buildReconciliationRows` (now reports quantity + Mother Coil trace + SKU conversion/ladder only) and dropped the Dashboard **Stock Value (INR)** CSV column. SKU conversion/ladder costs kept. **No DB migration** — `coils.cost_price`, `baby_coils.cost_price`, `purchase_orders.item_price/updated_price` left dormant (the generic `toSnake`/`toCamel` and its test still handle the keys; rows just stop being written). **Distributor codes:** new pure `distributorCode(name, words=2)` = first 2 words uppercased (`'—'` passes through); wired into the Sales distributor column + dropdown, SKU-breakdown title, open-order backlog, and dispatch/orders tables (full name kept on hover via `title` and in all CSV exports). | Removing a field that feeds a derived report means deciding the report's fate too — coil cost fed both Invoice Reconciliation and Stock Value, so those cost columns had to go with it (kept the non-cost columns). Auto-deriving a code from the name is zero-data-entry but collides when two distributors share their first 2 words; the `title` tooltip (full name on hover) keeps them distinguishable without a master list. 92 calc tests pass; `vite build` clean.

2026-06-24 | Dashboard + Sales | Feature — added **Reserved inventory** + "Available (Most Relevant)", reshaped the SKU views, made SKU tables Excel-like | New ERP order field **`Release Qty`** mapped in `mapOrderRow` (alongside `invoicedQty`). New pure helpers in `calc.js`: `reservedBySku(orders)` = Σ `max(0, releaseQty − invoicedQty)` over **open-status** lines (reuses `isOpenOrderStatus`, which already excludes Delivered/Cancelled/Rejected/blank — satisfies "not delivered/cancelled/nan"); `skuSizeLabel(sku, desc)` for the Size filter (`nominalBore`→"32 NB", else `height×breadth`→"150x150", desc fallback). `skuInventoryRows` now returns `production` (all-time, from `producedPool.producedWeight`) + `reserved` (live over all orders) and redefines **`free = inventory − reserved`** (was `inventory − pending`); keeps `totalOrders/totalInvoiced/pendingToInvoice` for the FG cards/CSV. `skuDemandSupply` attaches `reserved` + `available` (= inventory − reserved); `distributorSalesRows` per-SKU rows inherit them from `invByCode`. `DataTable` gained opt-in **`excel`** (cell borders + zebra) and **`maxHeight`** (scroll container so the existing `sticky top-0` thead actually stays fixed). Dashboard SKU table migrated from a hand-rolled `<table>` to `DataTable` (cols Production/Pending/Reserved/Inventory/Free + Type/Size/Inventory/Reserved filters); Sales SKU breakup adds Reserved + "Available (Most Relevant)" (replaces the old per-SKU Free Stock), filters, and a CSV download. | **A new order field needs a Supabase column** — orders sync via `useSupabaseStore('jsw:orders')` with top-level camel→snake, so `releaseQty`→`release_qty`; added it to the `orders` DDL + an idempotent `alter table orders add column if not exists release_qty numeric;` in `supabase-setup.sql`. Until that runs on the live DB (or the sheet is re-uploaded), reserved degrades gracefully to 0 (null `release_qty`). **Numeric "bucket" filters reuse the existing equality-dropdown mechanism** — no DataTable engine change; just pass an accessor returning a label (e.g. `r.inventory>0?'In stock':…`) with explicit `options`. Verified `reservedBySku` against the real `Orders_PT_23rdMay.xlsx`: 175 open lines → 25 MT across 4 SKUs (matches an independent recompute). 79 calc tests pass; `vite build` clean.

## Entries
(Add new entries at top)

2026-07-16 | Dashboard (FG inventory) + reports.js | **Over-dispatched SKUs (dispatched > produced → negative per-SKU inventory) were summed into the FG totals, dragging them below the real on-hand stock.** Per-SKU `inventory = producedPool.availableWeight` (`calc.js:262`, unclamped `produced − dispatched`) and `free = inventory − reserved` go negative when a SKU is invoiced beyond recorded production; those negatives were summed **unclamped** in three independent aggregates — the FG KPI cards (`skuTotals`, App.jsx), the SKU-table TOTAL row (`DataTable.totals`, App.jsx), and the MTD "Finished Stock" export grand/subtotals (`buildFinishedStockData`, reports.js). | Floored each per-SKU value at 0 **only where it is summed**, leaving the per-SKU value negative so the anomaly stays visible. App.jsx: `skuTotals` reduce now adds `Math.max(0, r.inventory)` / `Math.max(0, r.free)` (fixes **FG Left Inventory** + **Free FG** cards); `DataTable` gained an optional per-column **`totalValue(r)`** accessor (transforms the value for the TOTAL row only — never the per-row sort/filter/search/render), used by the **Inventory** + **Free Inventory** columns as `Math.max(0, …)`. reports.js `buildFinishedStockData`: the inclusion test changed `Math.abs(mt) > EPS` → `mt > EPS` (a negative-stock SKU is treated like zero — not listed), and the sub/grand reduces clamp `Math.max(0, …)` as a safety net; aligns the Finished-Stock sheet with the already-floored "Physical Inventory" sheet (`skuAgeing`, `calc.js:300,310`). | **A negative per-SKU inventory is a data/timing artifact, not physical stock — floor it at 0 at every SUM site, but keep it negative per-row so the over-dispatch alert + "Negative" filter still surface it.** The fix is at the aggregation sites, NOT `calc.js` `producedPool`/`skuInventoryRows` (those per-row values are relied on by the alert @App.jsx:1799, the "Negative" table filter, red highlighting, and negative-first sort). Two independent sums exist for the same rows (`skuTotals` → cards, `DataTable.totals` → table row) — both had to change; `fgReserved` and the Ageing TOTAL (`portfolioAge`, already weighted by `inventory>0`) were already safe. Verified against the real `skuInventoryRows` output: an over-dispatched SKU stays −3 per-row while the Inventory/Free totals move 5 → 8 (negative excluded). 169 tests pass (1 new reports test), `vite build` clean. Browser spot-check needs live Supabase data (absent in a fresh clone).

2026-07-15 | App-wide (auth) | Feature — added a login gate (one shared login id + password) in front of the previously wide-open app | The app had no auth: anyone with the Vercel URL had full UI access (and the anon key + open `using(true)` RLS meant the data was reachable regardless). Chose a **Supabase-stored gate** (over an env-var password baked into the bundle, or full Supabase Auth + RLS lockdown): new private table `app_credentials(login_id, password_hash)` with **RLS on + NO policy + privileges revoked from anon/authenticated**, so the hash is unreadable through the public key; a `security definer` function `verify_login(p_login_id, p_password) → boolean` (bcrypt via pgcrypto `crypt()`/`gen_salt('bf')`, `search_path = public, extensions`) granted `execute` to anon is the ONLY way in. Client: `verifyLogin()` in `db.js` wraps `supabase.rpc('verify_login', …)`; `App.jsx` split into an auth wrapper `App` (gates on `jsw:auth` localStorage w/ ~30-day expiry, applies the dark class on mount) + the renamed `InventoryApp` (gained an `onLogout` prop + header **Logout** button) + a new `LoginGate` form. Mirrored the DDL into `supabase-setup.sql` (seed left commented so no password enters git); seeded `admin` + a temporary password via MCP. | **A client-side gate on a static site guards the UI, not the data** — the anon key is in the bundle and the data tables stay open, so this is honestly scoped as "keep people out of the app," with Supabase-Auth-+-RLS lockdown documented as the upgrade path (`blueprints/manage-app-login.md`). Keeping the credential in a table the app itself can't read (the definer RPC returns only yes/no) is strictly better than an env-var/bundle password and needs no redeploy to change. Verified via Supabase MCP: `verify_login` → true/false/false for correct/wrong/unknown, and anon has no SELECT/INSERT on `app_credentials` (RLS on). Browser test (Playwright + pre-installed Chromium): the login screen shows first and the app is hidden pre-login; the browser→Supabase RPC round-trip couldn't run in-sandbox (the agent proxy 403s all outbound incl. `supabase.co`) — it works on Vercel. `vite build` clean.

2026-07-06 | calc.js — SKU identity (Pillar 1, Phase 2) | **Extended the canonical-identity join from the inventory bridge (Phase 1) to the dispatch coil trace, invoice reconciliation, and the Sales distributor SKU drill-down** — so a production/order/invoice for the same physical pipe reconciles even under a different code string across those surfaces too. Same read-time-resolution pattern (`skuKeyResolver`, no schema/backfill), same optional-`keyOf`-default-identity discipline so nothing existing changes on today's data. | `dispatchCoilTrace(…, keyOf = identity)` now matches production↔dispatch by `keyOf(skuCode)` (a dispatch coded differently from its production still inherits the right coil trace); `buildDispatchRecords` (`App.jsx`) builds `skuKeyResolver(skus)` once and passes it. `buildReconciliationRows` resolves the SKU's description/conversion/ladder via a `keyOf`-keyed master map (variant code still finds its master; falls back to exact `find`). `salesByDistributor(orders, dispatches, month, skus=[])` keys its per-SKU `_sku` drill-down by canonical identity with a representative real code for display; the Sales caller (`App.jsx` SalesDashboard, `salesByDistributor(orders, dispatches, month, skus)`) now passes `skus`. `skuKeyResolver` added to the App.jsx calc import. | **No-op on current data** (still 0 canonical collisions in the master, per Phase 1's live check — Supabase MCP was unauthenticated this pass so no fresh query, but the master hasn't changed the collision property). 147 prior tests pass unchanged + 3 new: `dispatchCoilTrace` inherits a production's coils for a variant dispatch code WITH `keyOf` (and returns empty WITHOUT, proving the raw path is what broke); `salesByDistributor` merges two same-pipe order codes into ONE drill-down row when `skus` is supplied (and keeps them split without). `vite build` clean. **Deferred to Phase 3:** source hardening — fix `SKUMaster` CHS description auto-derive so hand-created CHS get a strong key; update test-only aggregators (`skuBookingRows`/`skuDemandSupply`/`distributorSalesRows`/`orderBacklog`) for parity. Same known limit as Phase 1 (grade/finish not in the key).

2026-07-06 | calc.js / reports.js — SKU identity (Pillar 1, Phase 1) | **Made `canonicalSkuKey` the single join key for inventory netting, the Dashboard SKU table, and the Finished-Stock report — so the same physical pipe reconciles even when carried under different code strings (ERP code vs description vs `1.6`/`1.60`), and the two identifier systems (`skuCode` on productions/dispatches vs `mmId` on orders) finally bridge.** Root pattern behind the whole bug family (weight-lookup misses, phantom dispatch stock, stranded inventory, orphaned productions) is "same pipe, different string" under raw-`===` joins. | **Read-time resolution — no schema change, no backfill, no write-site stamping** (chosen over a stored `sku_key` column, which needs ALTERs + a backfill + can go stale; and consistent with the earlier "derive from the master, don't freeze" weight fix). New pure `skuKeyResolver(skus)` (`calc.js`) builds a `code → canonicalSkuKey(masterObject)` map (keys computed ONLY from full objects, satisfying the key's invariant) and returns `keyOf(code)`; an **unmatched** code keys as **itself** (strict superset of raw-string behavior — never wrongly merged). Threaded through as an optional param (default identity, so every existing test/caller is untouched): `producedPool(…, keyOf)` and `reservedBySku(orders, keyOf)`; `skuInventoryRows` (`calc.js`) now builds `keyOf` internally and keys ALL its maps (invoiced, ordered, pending, reserved, pool) by identity, resolving a representative SKU per key for display; `buildFinishedStockData` (`reports.js`) looks up `pool[keyOf(s.skuCode)]`. **Zero App.jsx changes** — the two entry points (`skuInventoryRows` @App:1667, `buildFinishedStockData` via the Reports button) already receive `skus`. Explicitly NOT touched (agent-confirmed): coil-keyed logic (`coilConsumption`, `coilInventoryRow`, `buildRawMaterialData`) and the order-line/distributor axis (`shippedByOrderLine`, `distributorOrderIndex`, `resolveDistributorIdentity`). | **Verified no-op on today's data:** a live Supabase check found **0** distinct published sku_codes that collapse to the same canonical key and **100%** of published SKUs carry an `IS ####` token → the change moves **no current number**; its value is future-proofing (it only kicks in when a mismatch WOULD strand stock). All 141 prior tests pass unchanged (the identity-default proof) + 6 new: `skuKeyResolver` resolution & unmatched-passthrough, `producedPool`/`skuInventoryRows` merging code-A-production with code-B-dispatch/order into ONE bucket/row (and the raw-string version still splitting them 2-ways), plus `canonicalSkuKey` edges (SHS object round-trip, integer-thickness `2→2.00`, length-distinct, and the **grade/finish-not-in-key** known limit pinned as a test). `vite build` clean. **Known limit documented:** the key omits grade & finish (`YSt 210`/`310`, Black/Galv) — safe for today's single-grade Black-MS catalog; add those segments if variants are introduced. **Deferred to Phase 2/3:** dispatch trace + reconciliation + sales drill-down (`dispatchCoilTrace`, `buildReconciliationRows`, `salesByDistributor`); then source hardening (fix `SKUMaster` CHS description auto-derive; test-only aggregator parity). **Durable rule: one physical identity, computed once from the master object, is the join — raw code strings are display labels, not keys.**

2026-07-06 | Stage 3 (Production) weight + SKU Master | **Production weight showed 0 for ~24 rows (~107 t; e.g. "40 NBx4") even though the SKU Master had the weight — the value was frozen at save-time and never recomputed.** `weightPerPiece`/`totalWeight` (and each `coilAllocations[].weight`) are computed once in `Production.save()` from `weightPerPieceFromSku(sku)`, which returns 0 on ANY lookup miss (SKU not created yet, keyed to a SKU made later, or blank `weightPerTube`); the records table/CSV/Dashboard/Coil Tracker then render the STORED value and never recompute, so a row saved before its SKU had a weight stays 0 forever. Confirmed the exact class on live Supabase: a production saved 2026-06-23 whose SKU row was created 2026-06-24; and the plant's own annotated Production CSV showing the correct Wt/Piece == the current SKU-master weight for each of the 5 SKUs (40 NBx4, 65 NBx4, 75x75x2.00, 75x75x2.50, 38x38x4.00). Root process = the bulk `scripts/import-excel.mjs` (`buildSkus` wrote null-weight master rows from a blank sheet weight column with no density fallback; `buildProductions` then froze `total_weight=0`, and for blank-code sheet rows stamped synthesised `SHS-HxBxT` codes). | **Fix = recompute-on-read, non-destructive.** New pure `resolveProductionWeights(productions, skus, babyCoils)` in `calc.js`: rewrites weightPerPiece/totalWeight/`coilAllocations[].weight` live from the CURRENT master, ONLY when the SKU resolves to a POSITIVE weight (unknown/unpublished/weightless/NaN → passthrough, never zeroes a good row); also re-derives a blank mother `hrCoilId` from the baby coil (closes deferred item **I** for the display) and, on a duplicate skuCode, prefers the positive-weight master row. Wired at `App` level (`resolvedProductions`) into ALL 7 read consumers (Dashboard, Coil Tracker, Coil Inward, Slitting, Production, Orders, Reports); mutations still write through the raw store so edited rows self-heal in the DB over time. `weightPerPieceFromSku` hardened to `Number.isFinite(n) && n>0` (no NaN reaches display). **Write-side prevention (closes deferred item E):** `SKUMaster.save` now BLOCKS publishing a SKU with `weightPerTube ≤ 0` (drafts may stay blank; they aren't selectable in Production); the Production form shows a red, non-blocking warning when the chosen SKU has 0 weight; `deleteSku` is guarded against deleting a SKU any production references; the SKU Code field is locked in edit mode (renaming orphans productions). **Data (live Supabase):** flipped 3 nominal-bore SKUs mislabeled `SHS`→`CHS` (`40 NBx4`, `65 NBx2.8`, `65 NBx4`); `38x38x4.00`'s weight was already restored (24.4). Post-fix verify: 0 unmatched production codes, 0 published-with-no-weight SKUs, 0 rows that stay zero. 141 calc tests pass (8 new), `vite build` clean. | **A weight stored at save-time is a cache that silently goes stale — render reference data (SKU weight) live and treat the stored copy as non-authoritative.** Recompute-on-read fixed all ~107 t with NO DB migration (the master already had the weights). The read-fix alone isn't foolproof: it cannot invent weight, so it's paired with write-side guards that stop weightless SKUs/productions at the source — three independent adversarial reviews all flagged `SKUMaster.save` (weightless publish) as the root enabler. The `canonicalSkuKey` fallback that looked obvious for the Production lookup does NOT work: productions carry only an opaque `skuCode`, no description for the canonical key to parse. **Documented but NOT changed this pass (recommended follow-ups; kept out to stay focused + not destabilise the reconciliation path):** (1) dispatch import stores an unresolved Item Name as a raw `skuCode` with 0 pieces → phantom `producedPool` netting, and a resolved-but-weightless SKU derives 0 pieces silently (both now largely mitigated because publishing weightless SKUs is blocked); (2) `producedPool` nets by exact `skuCode`, so a pre-existing duplicate master code still strands inventory (keep the master deduped / net by `canonicalSkuKey`); (3) Dashboard `downloadStockCSV` "Raw Remaining" doesn't exclude slit mothers (double-counts vs the KPI/report). A full division sweep across App.jsx/calc.js/reports.js found NO unguarded division (no NaN/Infinity reachable) — the codebase's discipline is that guards turn bad inputs into silent 0s, which is exactly the class that corrupts numbers without an error.

2026-07-05 | Sales dashboard + Orders/Dispatch importers + PO Master | **Feature — reworked the Sales dashboard to a Confirmed / Non-confirmed / Invoiced (MTD) model driven by ONE daily upload; removed PO Master, Open Order Backlog, and SKU Demand vs Supply.** The One Helix workbook's first two tabs are the source of truth: **Orders** tab → `orders` (each line gains `confirmed` = ERP "Release − Invoiced Qty" [col **BE**], `nonConfirmed` = "Ordered − Release Qty" [**BF**] − "total cancelled qty" [**BK**], read by header name with a BE/BF/BK **positional fallback** in `mapOrderRow(row, cols)`); **Invoice** tab → `dispatches` via the EXISTING One-Helix pipeline (extracted the former `Dispatch.onUpload` core into a pure module-level `buildDispatchRecords`). The standalone Orders + Dispatch uploaders collapse into one **"Upload Sales Excel"** button on the Orders (now "Orders & Invoice") tab; the Dispatch tab stays as a records/reconciliation VIEW. | New pure helpers in `calc.js`: `salesKpis(orders, dispatches, month)` (Confirmed=Σ`confirmed`, Non-confirmed=Σ`nonConfirmed`, Pending=C+NC, MTD Invoice=Σ current-month `bundleEntries.weight`, Total=Invoice+C+NC) — used by BOTH the Sales dashboard AND the factory Dashboard so the two screens can't diverge; `salesByDistributor` (reuses `distributorOrderIndex`/`resolveDistributorIdentity` so the "V V" merge still holds; carries per-SKU `skuRows` for the drill-down); `salesByMonth` (Confirmed/NonConf by order month, Invoiced by invoice month). Sales KPI cards → Total Orders / Pending to Dispatch / Confirmed / Non-confirmed / MTD Invoice; distributor table + a new **Month-wise Sales** table share those five; the factory Dashboard's Invoiced/Ordered/FG-invoiced cards are replaced by the same salesKpis row. **Invoice source = `dispatches` (single source of truth) — NOT a new store**, deliberately, to avoid recreating the 2026-07-01 2× double-count; the combined upload REPLACES dispatches (soft-delete prior + rebuild) so a re-upload can't accumulate. Supabase: `alter table orders add column if not exists confirmed / non_confirmed / distributor_code`. Removed PO Master (component, `mapExcelRow`, `jsw:purchaseOrders` store + `TABLE_MAP` + `TABLE_LABELS`, the Dashboard PO-ending alert); left the `purchase_orders` table dormant; left `orderBacklog`/`skuDemandSupply` in `calc.js` (now test-only). | **Verified E2E against the real file** via the actual `calc.js`: Confirmed 77.25 T, Non-confirmed 421.38 T, Pending 498.63 T, MTD Invoice (July) 78.35 T, Total 576.98 T; the distributor table Σ reconciles to the KPIs; "V V N STEELS" collapses to one row. 122 calc tests pass (4 new), `vite build` clean. **Durable rule: when the app already ingests a data source (`dispatches` IS the One Helix invoice), reuse it — a parallel store re-opens the exact double-count hazard it was built to prevent.** Reversed the earlier "no `distributor_code` on orders" note (2026-06-26): the column is now added intentionally with an idempotent ALTER, so orders carry the code directly (sharper identity grouping) while the order-link resolution still works as the fallback.

2026-07-01 | Importers (Dispatch / PO / Orders) | **June 30 dispatches looked "missing" — actually stored one day early (June 30 → June 29), and the whole date column was shifted −1 day in IST.** The One Helix invoice `JODLTE0626/00800` (June 30, 4 lines: RHS 100×50×3.20=12.2, CHS 32NB=6.1, CHS 20NB×2=5.2, CHS 20NB×1.60=5.9) imported fine but showed under 2026-06-29, so it dropped off the top of the date-sorted table. Root cause in `toISODate` (was in `App.jsx`): `XLSX.read(..., {cellDates:true})` returns a JS Date for a date-only cell aimed at the viewer's LOCAL midnight, but serial→date float rounding lands ~10 s short — in IST the cell came back as `2026-06-29T18:29:50Z`, which reads as the 29th with BOTH local and UTC getters. The prior "use local getters" comment was an earlier, insufficient patch for this same class of bug. | **Moved `toISODate` into `src/lib/calc.js`** (exported, now unit-testable) and changed its Date-object branch to **snap the instant to the nearest whole UTC day** (`new Date(Math.round(v.getTime()/86400000)*86400000)`) then read with UTC getters. Correct for any TZ within ±12 h of UTC (every business zone incl. IST +5:30). `App.jsx` now imports it; the 6 call sites (dispatch `dateOfDispatch`, PO `purchaseOrderDate`/`poEndDate`, orders `orderDate`/`expectedDeliveryDate`) are unchanged — so PO and order dates get the same fix for free. 4 regression tests in `calc.test.js` (incl. the exact IST instant → `2026-06-30`). Verified E2E via the real `calc.js` against the file under IST/UTC/PST: max date `2026-06-30`, invoice 00800 → `2026-06-30`. 118 tests pass, `vite build` clean. | **A date-only Excel cell is not an instant — snap it to a day, don't trust local getters on a near-midnight timestamp.** The bug is invisible in a UTC-run test/CI (local==UTC) and only bites in the operator's real timezone, so a regression test must construct the actual offending instant (`2026-06-29T18:29:50Z`), not a clean midnight. **Stored records imported before the fix keep the wrong date** — they're corrected by the next Replace-mode re-import, not retroactively. Audited the rest of the One Helix import against the real file (288 lines): 0 unresolved SKUs, 0 zero-weightPerTube, 0 invoice-spanning-multiple-dates, 0 within-file dupes, 0 blank customer/PO — the date parse was the only defect.

2026-07-01 | Stage 4 (Dispatch) | **~50 SKUs showed negative stock — the ERP invoice file had been imported twice, so dispatched tonnage was stored at exactly 2.00× reality.** Reconciled the SKU report against the invoice file: `Inventory = Production − Dispatched`, and dispatched was double, so ~1,257 T of phantom negative stock across 50 SKUs; the ratio `(Production − Inventory)/fileInvoiced` was a clean **2.00** for all 42 file-present SKUs. Root cause in `onUpload` (`src/App.jsx`): dedup was **per-invoice** via `existingInvoices = Set(dispatches.map(d=>d.invoiceNo))` and skipped a row only when `r.invoiceNo && has(...)` — so a **blank invoice number bypassed it entirely**, it never caught **duplicate lines within one file**, and the blank-invoice grouping key used the **row index** `i` (a re-upload minted fresh records). Separately the operator switched to a cleaner **"One Helix"** invoice export (Zoho) that carries the same 1,931.4 MT / 81 invoices but has **no MM ID, no Sku ID, no pieces** — only Item Name (== SKU description, resolves 100% by description), Quantity (MT), Invoice Number, Customer Name, PurchaseOrder (== the old Child Order ID). | Rewrote the importer for **One Helix only** + made it **idempotent per line** + a **Replace mode** for the one-time rebuild. New pure helpers in `calc.js`: `dispatchLineKey(line)` = normalised composite `invoiceNo | skuCode | weight(3dp)` (format-independent, so it dedups across the old ERP shape too — unlike a per-line Sku ID only the ERP file has), and `dedupeDispatchLines(existing, parsed)` → `{toImport, skippedDuplicateLines, skippedInvoices}` (scoped to **non-deleted** so soft-delete-then-reupload corrections still work; a `seenThisBatch` set kills within-file dupes). `mapDispatchRow` now maps One Helix headers (`itemname`→desc, unit-aware `quantity`→weight, `purchaseorder`→childOrderId); `onUpload` resolves SKU by description→canonical, dedups on the resolved lines, drops the row-index grouping fallback, and the summary reads `0 new lines — N duplicate line(s) skipped` on a clean re-upload. **Replace mode** soft-deletes current records then imports fresh (guarded confirm; reversible). Verified E2E against the real file: 288 lines / 1,931.4 MT import once, a second upload imports **0** (288 skipped). 114 unit tests pass (8 new), `vite build` clean. | **Dedup at the granularity of the thing that double-counts — the LINE, not the invoice — and never let a fallback key depend on row index.** Deliberately did **not** add a DB unique constraint: dispatch lines live nested in a `bundle_entries` JSONB array (no per-line column to constrain), and an invoice-level index would fight the optimistic upsert-on-`id` sync + the soft-delete rebuild. **Impact check (the ask):** the negative-stock report, Coil Tracker, Dashboard KPIs, Invoice Reconciliation + CSV, and cost reconciliation are **unaffected** (they read skuCode/weight/date/coilAllocations, all present or derived). Only the **Sales Dashboard / Order Backlog** degrade slightly — the One Helix file lacks the per-order-**line** `Sku ID`, so `shippedByOrderLine` (which has an `if(lid)` guard, calc.js:322 — lines are skipped, NOT mis-bucketed) loses per-line netting; but `orderLineInvoiced` falls back to the order sheet's own `invoicedQty` and distributor grouping is preserved via `childOrderId` (from PurchaseOrder → `resolveDistributorIdentity` byChild), so customer- and per-order totals stay right — only the split within a multi-line order loses precision. Applied the gstack office-hours (narrowest wedge: switch to the clean source) + plan-eng-review (blast-radius impact map, reversible replace, failure modes) lenses while planning.

2026-06-26 | Stage 3 (Production) | **Audit follow-up — fixed the highest-priority Stage-3 misses** found after the "blank allocated" bug. User scoped to: (A) the searchable picker only committed on an option *click* — typing a coil/SKU name and pressing Enter or clicking away silently lost the choice (the original bug's mechanism); (B) over-allocating (assign more pieces than produced) saved with a green "Allocated" status; (C/D) the per-row pieces and "No. of Pieces" inputs accepted negative/decimal values (fractional tubes; negative counts shown in the form total but dropped on save). Decisions: keep **warn-never-block** (don't block over-allocation, just label it honestly) and make the picker smarter. | All in `src/App.jsx`, no schema/`calc.js` change (86 unit tests stay green). (A) `SearchSelect` (~`:104`) gained keyboard support — ↑/↓ highlight (`activeIdx`), **Enter** commits the highlighted/first filtered option, **Esc** closes; a conservative blur-commit fires only on an EXACT case-insensitive label match (so a partial query isn't surprise-selected when clicking away); and the display falls back to the raw `value` when it isn't in the current option list, so a picked coil never renders blank. choose()/click behaviour unchanged → backward-compatible for the SKU picker too. (B) `save()` status (~`:845`) adds an `over` state (`allocPcs > pieces ? 'over' : …`) and the Production Records Status column (~`:876`) renders it as a red "Over-allocated" badge; saving stays allowed (`canSave` untouched), the in-form "Over-assigned" warning is unchanged, and the Dashboard shortfall alert (filters `partial`/`unallocated`) correctly ignores `over`. (C/D) `pieces` derivation (~`:704`), `setRow` (~`:806`) and the "No. of Pieces" onChange all clamp to `Math.max(0, Math.floor(Number(...)))`; both number `<Input>`s got `min="0" step="1"`. Verified end-to-end in Chromium (Playwright, `--mode test`): SKU+coil select via Enter; `2.5`→`2`, `-3/-4`→`0`; produce 5 / assign 7 → saves with Status "Over-allocated". 86 tests pass, `vite build` clean. | **Deferred (known, by user's "highest-only" scope):** E — a SKU with no `weightPerTube` saves 0-weight allocations with no warning; F — a picked baby coil drops out of `babyCoilOptions` once its free capacity hits ≤0.02 MT (filter `o.free > 0.02`), so mid-edit it can't be re-confirmed; G — the same baby coil can be added in two rows (duplicate allocations); H — the picker's "free MT" label doesn't subtract pieces assigned in OTHER rows of the current unsaved form; I — if a baby coil is missing at save/enrich time the mother `hrCoilId` saves as `''` and silently drops from the Coil Tracker / cost reconciliation (largely prevented by the Slitting delete-guard, realistic mainly for imported/edited history); J — `calc.test.js` lacks edge-case tests (negative/zero pieces, empty coils, zero wpp) and `e2e/pipeline.spec.js` is stale (drives the SKU `SearchSelect` via native `selectOption`). Durable rule reaffirmed: a search-combobox that commits only on click is an error-trap; give it Enter/exact-match + keyboard nav AND make the on-screen "allocated" accounting use the SAME predicate `save()` persists.

2026-06-26 | Stage 3 (Production) | **Operator selects + allocates a baby coil and saves, but the record shows the Allocated column blank / Status "Unallocated"** — user report "I am saving the baby coil but it is not showing and it is not getting allocated." Reproduced end-to-end in a real browser (Playwright, `--mode test`): Stage 1 coil (Actual Wt 10 T) → Stage 2 slit baby `HYD-0626-01-A` (10 T, ✓-matches the SKU) → Stage 3 production. The slit/FIFO/picker were all fine (baby showed in the table, FIFO suggested it `× 5`, the picker flagged it `✓`). | Root cause: a **display-vs-save mismatch** in `Production`. The form's allocation summary counted pieces from **every** editable row — `allocatedPieces = enriched.reduce((s,r)=>s+r.pieces,0)` (`src/App.jsx`) — but `save()` only persists rows that also have a coil: `enriched.filter(r => r.babyCoilId && r.pieces>0)`. So a row with **pieces typed but no coil actually picked** showed the green badge **"✔ Fully allocated across 0 coil(s)"** and `Allocated (pcs) = 5/5`, then saved with `coilAllocations: []` → record renders `0 / 5`, Assigned Coils `—`, Status `⚠ Unallocated` (exactly the blank column in the user's screenshot). The trap is easy to hit because `SearchSelect` only commits a value on an **option click** (`choose()` → `onChange`); typing the coil ID and clicking away leaves `babyCoilId=''`. Fix (`src/App.jsx` `Production`, no schema/calc change): (1) `allocatedPieces` now counts **only rows with a `babyCoilId`**, so the form's "Allocated" count + the "Fully allocated" badge can never claim coil-less pieces (kills the "across 0 coil(s)" badge by construction); (2) added `unpickedRows` + a per-row amber **"⚠ Select a coil from the list"** pill (replacing the misleading green `0.0T·0%` capacity pill) and a summary badge **"A row has pieces but no coil selected — … rows without a coil are NOT saved"**. Verified: control flow (coil clicked) still saves `5/5` `✔ Allocated`; broken flow now shows `0/5` + the two warnings instead of "Fully allocated"; 86 unit tests pass (no test changes needed), `vite build` clean. | **The form must account for allocation EXACTLY as `save()` persists it.** When a summary metric (`allocatedPieces`) and the persistence filter disagree on what "allocated" means, the UI silently lies and the saved record contradicts the green badge the operator trusted. The durable rule: derive the on-screen status from the **same predicate** `save()` uses (`babyCoilId && pieces>0`), not a looser one. Secondary lesson: a search-combobox that commits only on click needs a **loud per-row signal** when a row is half-filled (pieces, no coil), because users type-then-click-away and assume it took. Repro method that nailed it: drive the actual React app in Chromium with the dummy `.env.test` creds (in-session optimistic state works even though Supabase writes fail), entering all three stages by hand — the discrepancy only shows through the real UI, not a unit test of the pure helpers.

2026-06-25 | Stage 3 (Production) | **FIFO was auto-allocating instead of honoring the operator's manual coil pick** — user reported "when we select a coil it is not allocated, and on saving all the FIFO is still allocated." Also wanted suggestions limited to genuinely usable coils. | Root cause: the editable allocation was **seeded from FIFO** — `const rows = manualAlloc ?? fifoRows` and `baseRows = () => (manualAlloc ?? fifoRows)…`, and `save()` persists whatever rows are shown. So a fresh form saved the **entire FIFO suggestion**, and the first manual edit baked **all** FIFO rows into `manualAlloc` alongside the operator's pick. Fix (in `src/App.jsx` `Production`): made the operator's selection the **single source of truth** — `rows = manualAlloc ?? []` (default **empty**, never auto-FIFO); FIFO is now a **read-only suggestion** with a **"Use suggestion"** button (`setManualAlloc(fifoRows.map…)`) + a **"Clear"**. Smarter suggestions per the user: new pure helper **`requiredStripWidth(sku)`** in `calc.js` (geometry only — `2×(H+B)` for SHS/RHS, `π×OD` for CHS, `0`⇒unknown) drives a **±5 mm width** pre-filter on the FIFO adapter (`babyAsCoils`), so Production eligibility is now **width ±5 mm AND thickness ±0.3 mm** (the ±0.3 thickness band stays inside the untouched `coilFifoAllocate`). The manual picker (`babyCoilOptions`) now lists only coils with **>0.02 MT free**, flags ✓ for coils matching **both** width & thickness, and shows **thickness + width** in each label. Each editable row carries a stable `_rid` (keyed on it, not the array index) so the picker reliably shows the chosen coil as rows are added/removed. 84 unit tests pass (4 new `requiredStripWidth`), `vite build` clean. | **A "suggestion" that pre-fills the same state it saves IS the saved value** — the bug wasn't the FIFO math, it was that `rows` defaulted to `fifoRows` and `save()` read `rows`. Separating *suggestion* (display + opt-in "Use") from *allocation* (`manualAlloc` only) is the durable rule: **the FIFO/coil suggestion is guidance only and must never auto-save.** Width filtering was done by **narrowing the coils handed to `coilFifoAllocate`** rather than touching the engine, keeping it generic and its tests byte-identical. The strip-width formula reuses the **same perimeter geometry already embedded in the catalog weight** (`A = t·perimeter`), so it's catalogue-consistent and **not** a density constant (CLAUDE.md-compliant); `0`⇒unknown degrades gracefully to thickness-only, and the manual picker always lists every >0.02 MT coil so an off-spec pick is never blocked even if the width window is slightly off. Note: `e2e/pipeline.spec.js` is **stale** (drives the SKU field via native `selectOption`, but the app uses the custom `SearchSelect`; its `/No eligible baby coil/` assertion predates the current badge text) and can't run in-session — the vitest suite is the gate.

2026-06-24 | Dashboard (SKU inventory) + SKU Master | Diagnosed **142.4 T of negative SKU inventory across 20 codes** (dashboard showed reds despite a **+840 T** overall surplus) against the live production export + the June ERP invoice (245 product lines). Split: **Reason #3 — decimal-format code mismatch (36.5 T, 4 SKUs)** and **Reason #2 — missing/under-recorded production (105.9 T, 16 SKUs)**; the FIFO/netting math was correct (Reason #1 ruled out). | Root cause of #3: the live `skus` table holds the **same physical product under two codes** whose descriptions differ only by decimals — a short form (`…100x50x1.6x6000`) that **production** picked, and the padded canonical form (`…1.60x6000`, e.g. `1140-13075-10074982`) that **invoices** use. Inventory nets by `sku_code` (`producedPool`), so the produced tonnage strands under the short code and never offsets the invoice → false negative (≈87 T booked under 5 off-master short codes). Fix is at the **source, not the matcher**: (1) new pure helper `canonicalSkuKey(skuOrDesc)` in `calc.js` — `type|IS-std|sizeLabel|thickness.toFixed(2)|length` (reuses `skuSizeLabel`; **includes the IS standard** so IS 1161 vs IS 3601 never merge); (2) **guardrail** in `SKUMaster.save` blocks creating a SKU whose canonical key already exists, and the **dispatch importer `resolve`** now falls back to a canonical-key match (`byKey`/`defByKey`) so an ERP line with `…1.6` resolves to the existing `…1.60` code instead of minting an off-master dup; (3) one-time `scripts/dedupe-sku-master.sql` (description-keyed, transactional, idempotent) remaps `productions.sku_code` short→canonical and sets the dup to `status='draft'` — clears all 4 false negatives, recovers ~87 T. Reason #2 gets a durable signal: a new **"dispatched beyond recorded production"** dashboard alert (reuses `skuRows`). 4 new `canonicalSkuKey` tests (80 pass), `vite build` clean. | **Decimal formatting is a silent SKU-identity splitter** — "1.6" vs "1.60" are different `sku_code`s for the same tube, so any code-keyed netting fragments. Fix identity at **write/ingest** (guardrail + canonical resolve + DB dedupe), NOT by special-casing decimals inside one read path — `producedPool`/cost/`reservedBySku`/the picker all key by code and would each need patching otherwise. The canonical key **must include the IS standard**: the only signature-collisions in `skus.js` were legit IS 1161 vs IS 3601 pairs, which must stay distinct. The production CSV export shows **descriptions** (`skuDesc(code)`), not codes — so diagnosing by description, then signature (decimal-immune), separated true shortage (105.9 T) from the format artifact (36.5 T); the per-code totals matched the screenshot exactly, confirming the dataset. **`skus` is soft-delete and the picker filters `status==='published'`**, so retiring a dup = `status='draft'` (reversible), and only `productions` needs remapping (dispatch already uses the canonical MM ID; `baby_coils` carry no `sku_code`).

2026-06-24 | Stage 3 (Production) | User asked to drop the **dispatch-strand guards** — Edit refused to save ("Reducing this production would leave more pieces dispatched than produced…", greyed-out Update via `canSave`) and Delete `alert`ed "Cannot delete: dispatches for this SKU already use more pieces than would remain produced." Both block routine corrections when dispatches were uploaded before/independently of production. | Removed the guard end-to-end in `src/App.jsx` `Production`: deleted `dispatchedForSku`/`wouldStrandDispatches` helpers and the `editStrands` derivation, so `canSave = !!form.skuCode && pieces > 0` and `softDelete` just `confirm`s + soft-deletes. Dropped the now-orphaned `editStrands` warning badge and the unused `producedPool` import (it was only used by these guards — no other call site in App.jsx). `dispatches` stays in the `Production({…})` signature (still passed at the call site) but is now unused inside. Tests 72/72 + `vite build` green. | Same `wouldStrandDispatches` predicate powered **both** surfaces (edit-block + delete-alert), so one removal cleared both messages — grep the predicate, not the message strings. Removing a guard can strand an import: `producedPool` fell out of use entirely, so prune the import too (Vite won't error on it, but it's dead weight). This is **allow-and-warn** taken to its logical end — Production already never blocked on coil shortfalls; the dispatch-vs-produced invariant is now the user's responsibility, not the form's.

2026-06-23 | Data import / Stage 3 (Production) | Resync from a corrected workbook (`…v3_1.xlsx`) — 27 baby coils were missing from Coil-to-Slit; also showed the baby coil as `…-A×124` instead of just the id | Diff v3_1 vs v3: **baby_coils +27** (mothers HYD-0626-174/198/202, A–I; 0 removed, 0 existing widths changed → purely additive) and **productions 990→1028 rows** but inserted **mid-sheet** (diverge at row 983). Gave `scripts/import-excel.mjs` two emit flags: **`--tables=baby_coils,productions`** (skip unchanged skus/coils) and **`--rebuild-productions`** (prefix `delete from productions;` before the insert). Generated SQL = baby_coils upsert (1174, keyed by `baby_coil_id` so the 27 add and 1147 no-op) → `delete from productions;` → productions insert. App: `src/App.jsx` Assigned-Coils column now `map(a => a.babyCoilId || a.hrCoilId)` (dropped `×${pieces}`). | **Why productions need a rebuild, not an upsert:** their deterministic id is `detUuid('prod:'+rowIndex)`, so a mid-sheet insertion shifts every later row's id — a plain re-upsert would duplicate. baby_coils/coils/skus are keyed by natural ids (`baby_coil_id`/`hr_coil_id`/`sku_code`) so they re-import idempotently with no rebuild. Surprise in the data: of the 38 new production rows only ~4 loaded — **34 have a blank SKU** in the sheet (baby coil + pieces entered, SKU left empty), so they can't be weighted and are skipped (48 blank-SKU total). The new mothers' 27 babies are now in inventory but **no production consumes them yet** (their would-be production rows are among the blank-SKU ones).

2026-06-23 | Stage 4 (Dispatch) | Live upload of the ERP file failed with **"Could not find the 'customer' column of 'dispatches'"** — the importer wrote `customer` as a **top-level** dispatch field, but the `dispatches` table has no such column, so Supabase rejected all 66 rows (nothing persisted on refresh). Separately, the records table crammed every SKU of an invoice into one comma-joined cell. | Moved `customer` **off the record top level and into each `bundleEntries[]` entry** — JSONB, schema-less, the same place `grade`/`diameter` already persist — so `toSnake` no longer emits a top-level `customer` key and the upsert succeeds with **zero schema migration**. `buildReconciliationRows` now reads `entries[0]?.customer` (keeps the `d.customer` fallback for legacy rows); the Customer table column reads `bundleEntries?.[0]?.customer`. For display, the Dispatch table's **SKUs / Pieces / Weight** columns now render **stacked per-SKU lines**: a `skuLines(r)` helper aggregates an invoice's entries by `skuCode`, and a `stack()` helper maps each to its own `<div>` so the three columns align row-by-row. Each column keeps a string/number `value` so DataTable **search & sort still work** (it uses `value` for filter/sort and `render` only for display). Verified with the importer sim against the real file (66 invoices, every entry carries customer, **no** top-level `customer` on any record, a 5-line invoice → 5 stacked SKU lines) + 43 unit tests + `vite build`. | **JSONB is the migration-free escape hatch**: anything that doesn't map to a real `dispatches` column must live inside `bundle_entries` — the top-level-only snake_case conversion means a stray top-level key → "column not found" on sync, silently losing data until the next refresh exposes it. The 66 errored rows **never persisted**, so after deploy the user must **re-upload once** (dedupe won't skip them — nothing is in the DB yet). And a DataTable column can carry **both** `value` (search/sort) **and** `render` (display) — use that to get rich/stacked cells without losing search or sorting.

2026-06-23 | Stage 4 (Dispatch) | Adapted the dispatch Excel importer to the **real ERP invoice export** (53-col daily file) — old importer expected a placeholder format (date×vehicle grouping, fuzzy SKU match, always-append) | Re-mapped `mapDispatchRow` to the ERP headers: `Invoice date`→date, `Invoice number`→invoice, **`MM ID`→SKU (== `skuCode` exactly)**, `MM Description`→description fallback, `Invoiced qty`→weight (MT), `Distributor Name`→customer, plus `Grade`/`Diameter mm`. The file has **no vehicle and no pieces** columns — pieces are derived from weight via `SKU.weightPerTube`. Grouping changed from (date×vehicle) to **one dispatch per invoice** (verified: no invoice spans two dates; 66 invoices / 235 product lines). **Skip-dedupe** by invoice number (file is re-uploaded daily and overlaps months). The **`Freight` line** (`MM ID 9000000`, qty 0) is filtered explicitly. SKU resolution is now **exact** (MM ID→`skuCode`, then description) — dropped the loose substring fallback (it mis-costs). **Self-heal:** when a file references a `skuCode` that the live `skus` store lacks but `DEFAULT_SKUS` knows, the importer adds it via `setSkus` (persists to Supabase by `id`), so the 15 catalog sizes missing from the file resolve immediately and future uploads cost correctly. Added **15 new SKUs** (SKU-233..247: RHS 100×50×*, SHS 30×30/38×38×3.20/60×60×*, RHS 75×25×2.80, CHS 20 NB) to `DEFAULT_SKUS` via a new offline generator `scripts/generate-skus.mjs`. Dispatch table: Vehicle/Variance cols → **Customer**; reconciliation CSV gained **Customer + Grade**; `buildReconciliationRows` threads `d.customer`/`entries[0].grade`. `toISODate` got a guarded Excel-serial fallback. Verified end-to-end against the real file (66 records, 0 unresolved, 15 SKUs added, Freight excluded, re-upload skips all 66) and `vite build` passes. | **MM ID == skuCode was the unlock** — turns SKU matching into an exact lookup and makes the self-heal trivial (look the missing code up in `DEFAULT_SKUS`, push via `setSkus`). The **steel-density weight formula stays in `/scripts`, never `src/`** (CLAUDE.md "no density constants") — reverse-engineered & verified to reproduce existing catalog weights exactly: SHS/RHS `7850×(2t(H+B)−4t²)/1e6×L`, CHS `7850×π·t·(OD−t)/1e6×L` (NB→OD derived from existing CHS rows); `thicknessExtra` ladder t≤1.2→1000 / ≤1.6→750 / ≤2.0→500 / else 0. `DEFAULT_SKUS` is a fallback **only when the `skus` table is empty** (`db.js:83`) — editing it alone won't reach a populated live DB, so the importer must push new SKUs through `setSkus`. Dedupe is **skip, not upsert** (user choice): to re-import a corrected invoice, delete its record first.

2026-06-22 | Stage 3 (Production) | Made the FIFO baby-coil assignment **editable**, and added a **97% auto-advance** to the FIFO fill | `coilFifoAllocate` (calc.js) gained a `softFill` param (default **1.0** = unchanged, so all existing callers/tests are byte-identical) turning the fill into 3 passes: fill each coil to `softFill×capacity` (0.97 from Production) advancing oldest-first, then top up to 100%, then the existing 100→105% over-fill band. Production now passes `softFill: 0.97`. The read-only "Assigned Baby Coils" chips were replaced with **editable rows**: a `manualAlloc` state (`null` ⇒ follow live FIFO; else `[{babyCoilId,pieces}]`), a per-row baby-coil `Select` (`babyCoilOptions` = eligible ±5%-thickness coils labelled with free capacity, mirroring Slitting's `coilOptions`), a pieces `Input`, a per-coil capacity badge (green ≤97% / amber ≤105% / red >105% via cumulative `consumedByCoil` + this production's rows), and **+ Add coil** / **↻ Reset to FIFO** buttons. SKU change resets `manualAlloc`; `startEdit` seeds it from the saved `coilAllocations`. `save()` builds `coilAllocations` from the enriched rows (drops empty/0-piece rows, derives mother `hrCoilId`, `weight = pieces×wpp`); status = allocated/partial/unallocated from Σ pieces vs `tubeCount`. | **Warn, never block** stayed the rule: over-100%/over-105% and Σ≠tubeCount show amber/red badges but `canSave` is unchanged (`skuCode && pieces>0 && !editStrands`). Backward-compat trick: defaulting `softFill=1` kept the 40 existing calc tests green and only Production opts into 97% — added 3 targeted softFill tests (43 pass). Persistence needed **no** schema change — the edited `coilAllocations` array round-trips through the JSONB column (top-level-only snake_case). E2E couldn't run in-session (Playwright browsers need an egress-blocked download) — preserved the exact "No eligible baby coil" badge text so `pipeline.spec.js` assertions still hold; verified via unit tests + build + reasoning.

2026-06-22 | Data Import (one-time) | Bulk-load historical ops data from `Pipes_and_TubesOperations_v3.xlsx` into Supabase — there is **no bulk-import UI** for Coil Inward / Slitting / Production / SKU Master (only manual forms), and the app is Supabase-backed | Built `scripts/import-excel.mjs` (Node ESM) that **reuses the app's real `src/lib/calc.js`** (`coilFifoAllocate`, `coilConsumption`, `weightPerPieceFromSku`) so the production FIFO replay is identical to the Production form. Reads `xlsx`, recomputes baby weight/cost width-proportionally per mother (App.jsx slitting logic), **re-runs FIFO** chronologically (per locked decision — ignores the sheet's "Baby Coil ID" column), maps production SKU **description → sku_code**, and upserts `skus → coils → baby_coils → productions` (natural-key conflict targets; deterministic ids → idempotent). `--dry-run` stages `.workspace/*.json`; `--write` reads creds from gitignored `.env.local`. Validated result: 239 SKUs, 204 coils (1 `Void` excluded), 1147 baby coils, 921 productions (831 allocated / 3 partial / 87 unallocated). | Gotchas that mattered: (1) **Dates are mixed** — most cells are Excel serials but a late batch is `DD-MM-YYYY` **strings** (Indian order) that `new Date()` misparses → null → wrong FIFO order; parse day-first explicitly. (2) **Dedup SKUs by code, but keep EVERY description in the lookup** — several descriptions share one `sku_code` (and `sku_code` is UNIQUE), so deduping the insert list is fine but the description→SKU map must include all rows or productions go unmatched. (3) Six RHS 100x50 SKUs have full specs but a **blank code** in the sheet → synthesize the app's `TYPE-HxBxT.tt` code. (4) A safe **length-typo fallback** (match ignoring the trailing `x6000` token when unambiguous) auto-resolves `…x6001/…x6002`. (5) 55 rows / 5 SKUs (CHS 20 NB ×3, SHS 30x30x2.50, RHS 40x20x3) are genuinely absent; the user approved **deriving their weight-per-tube from steel density** — and reverse-engineering the existing master proved it was built with exactly that: ρ=7850 kg/m³, `A = 2t(H+B)−4t²` (SHS/RHS) / `A = π·t·(OD−t)` (CHS, OD from the sheet), `weight = A·L·7.85e-6` — reproduces stored weights to ~1e-15, so the 5 derived values are catalogue-consistent (helper lives in the **importer only**; the app still reads `weightPerTube`). Reusing the pure `calc.js` instead of re-porting FIFO guaranteed parity with the live app. **Egress pivot:** this web session's network policy blocked outbound to `*.supabase.co` (`Host not in allowlist`), so the `--write` path is unusable here — added a `--sql` mode that emits one idempotent `INSERT … ON CONFLICT` script to run in the Supabase SQL Editor (server-side, no egress). Date typos in the sheet were corrected on the user's confirmation: 60 baby-coil `…2027` dates → 2026, and one production `2026-01-06` → `2026-06-01`. Allocation method flipped twice with the user: FIFO-replay (865/4/107) was built first, but the user ultimately chose to **preserve the sheet's baby coil** — each production is attributed to EXACTLY the Baby Coil ID in its row (one allocation, no recompute/spill), baby coil as the consumed unit + its mother for costing. Final: **974 allocated / 2 unallocated** (sheet's baby cell blank) / 14 skipped (blank SKU) = 990. Lesson: when the source already records the actual coil consumed, preserving it beats re-deriving — re-running FIFO over history strands rows whose ±5%-thickness coils were already consumed, which doesn't reflect what really happened. The `--sql` emitter made the flip cheap: only `buildProductions` changed; everything downstream (idempotent upsert, validation) was reused. **Deployment gotcha:** the live Supabase predated the `productions` table, so the SQL errored `42P01: relation "productions" does not exist` at that insert (the whole `begin…commit` rolled back → DB unchanged; the earlier-parsed inserts confirmed skus/coils/baby_coils exist). Fix: made the import **self-bootstrapping** — prepend an idempotent schema preamble (the 4 `create table if not exists` + skus `add column if not exists` migrations + `enable rls`/`Allow all access` policies, lifted from `supabase-setup.sql`, inside the same transaction) so a single file works regardless of which tables pre-exist. Lesson: don't assume the deployed schema matches the repo's setup SQL — bootstrap idempotently rather than instruct the user to run a second file.

2026-06-22 | Pipeline (App-wide) | Process change — **re-introduced the Slitting stage** (mother coil → baby coils) before Production; Production now FIFO-consumes **baby coils**; **Bundle Formation removed**; **Dispatch uploaded from Excel** | New flow: Coil Inward → **Slitting** → **Production** → **Dispatch (Excel)**. Re-enabled the `jsw:babyCoils` store (`TABLE_MAP` + `HARD_DELETE_TABLES` in `db.js`; kept the `baby_coils` table and removed the `delete from baby_coils;` wipe in `supabase-setup.sql`). Restored `Slitting` from the old `CoilToSlit` (recovered via `git show cbe6756~1:src/App.jsx`): manual mother-coil pick, width-proportional weight/cost, letter-suffixed baby IDs, hard-delete (frees the letter), blocked once a production consumed the baby coil. Production FIFO now runs on **baby coils** via a small **adapter** (baby → `{hrCoilId: babyCoilId, actualWeight: weight, dateOfInward: dateOfConversion, thickness}`) fed to the **unchanged** `coilFifoAllocate`; allocations are **enriched** to `[{babyCoilId, hrCoilId(mother), pieces, weight}]`. `coilConsumption` gained a `key` param (`'babyCoilId'` for capacity; legacy mother-only rows skipped). Removed `BundleFormation`; `bundleCoilTrace`→`dispatchCoilTrace` (drains production FIFO by SKU, skipping already-dispatched); `producedPool` now produced − dispatched; `coilInventoryRow` dropped the bundled stage (Inward → Produced → Dispatched, new signature `(coil, dispatches, productions)`). Dispatch rewritten as an **Excel upload** mirroring the PO Master importer (`toISODate`, case-insensitive `pick()`, dynamic `import('xlsx')`); rows grouped into one dispatch per (date × vehicle); coil trace inherited from production FIFO; reconciliation CSV + records table unchanged. Dashboard/Coil Tracker reworked to the 3-stage view. Tests: `calc.test.js` updated for the new API (40 pass); `e2e/pipeline.spec.js` rewritten for the 5-tab flow. | **Adapter + enriched allocation is the key**: carrying BOTH the baby id (capacity/FIFO) and the mother id (cost/tracker) kept the whole downstream chain backward-compatible and let `coilFifoAllocate` stay generic (no change). Baby vs mother **cost-per-MT are identical** because both are width-proportional, so reconciliation keeping the mother rate is exact. Restoring removed UI from git history was far faster than rewriting. Baby coils were **re-enabled, not re-seeded** — rows still in Supabase reappear once the store is re-wired (the wipe is a manual one-time block that may never have been run).

2026-06-18 | Pipeline (App-wide) | Process change — added a **Production** stage + **FIFO coil attribution**; operators no longer pick a mother coil by hand at any step | New flow is 4 stages: Coil Inward → **Production** → Bundle Formation → Dispatch. Added a `productions` store/table (`jsw:productions` in `TABLE_MAP`; soft-delete) whose records carry `coilAllocations` (JSONB `[{hrCoilId,pieces,weight}]`, camelCase inner keys like `bundle_entries`) + a `status`. **Production is the coil-consumption point**: on save, `coilFifoAllocate` (new pure helper in `calc.js`) distributes produced pieces across coils — oldest `dateOfInward` first, eligible only within ±5% thickness, filling each coil to nominal `actualWeight` then stretching into the ±5% band; allocates whole **pieces** to avoid fractional tubes; leftover → `shortfall` (saved as `partial`/`unallocated`, **never blocked** — allow-and-warn policy). Bundle Formation now packs the **produced pool** (`producedPool`: produced − bundled per SKU) and inherits the coil split via `bundleCoilTrace`; the manual coil dropdown + `addSource` multi-row flow were removed (one record per bundle, accordion shows the derived split). Dispatch gained **multiple invoices per vehicle** (per-entry `invoiceNo` inside `bundle_entries` JSONB — **no DDL change**; one weighbridge reading) via an **invoice-first** form (`form.invoices` blocks: + Add Invoice → add bundles per block; flattened to per-entry `invoiceNo` on save). Extended `buildReconciliationRows` to weight-average cost across each entry's `coilAllocations` and group by `(invoiceNo, SKU)`; `coilInventoryRow` gained a 4th `productions` arg + produced/`balanceToProduce` columns. Dashboard/Coil Tracker added a Produced stage; alerts repointed (production shortfall replaces bundle over-fill). Guards: can't delete a coil consumed by production; can't shrink/delete a production below already-bundled pieces. | **Additive & backward-compatible** is what made this safe: every consumer reads `coilAllocations` with a fallback to legacy `hrCoilId`/`traceHrCoilId`, so old bundles/dispatches keep rendering and costing. `db.js` case-conversion is **top-level only** — JSONB inner keys must be authored camelCase. The integer-piece allocation (not raw weight) is the key numerical decision: each allocation's `weight = pieces × wpp` is exact, so Σ allocations == produced with no float dust. The whole helper chain (FIFO → consumption → pool → inherit → cost) is unit-tested in `calc.test.js`; the E2E `pipeline.spec.js` was rewritten for the 4-stage flow.

2026-06-15 | Pipeline (App-wide) | Process change — removed the "Coil to Slit" and "Slit to Tube" stages; collapsed 5 stages → 3 (Coil Inward → Bundle Formation → Dispatch) | Deleted the `CoilToSlit` & `SlitToTube` components, their tabs/routes, and the `jsw:babyCoils`/`jsw:tubes` stores (also removed from `db.js` `TABLE_MAP` and `HARD_DELETE_TABLES`). Bundle Formation now sources from a **mother coil + manually-chosen SKU**: `weightPerPiece = SKU.weightPerTube/1000`, with a weight-based per-coil cap (`Σ bundle totalWeight ≤ coil.actualWeight`, ±5% over-fill ceiling). Bundle records carry `hrCoilId` (was `babyCoilId`); dispatch entries carry `traceHrCoilId` (was `traceBabyCoilId`), so Dispatch reconciliation traces bundle→coil directly. Dashboard & Coil Tracker reworked to a 3-stage view (dropped Slit Stock/Tube WIP cards, Slit/Tube journey sections, recalculated stock/yield/CSV from bundles). Added `bundles.hr_coil_id` column + one-time `delete from tubes; delete from baby_coils;` wipe in `supabase-setup.sql`. | Re-keying the trace from `traceBabyCoilId`→`traceHrCoilId` is what lets every downstream view work without the middle stages. Legacy pre-wipe bundles/dispatches (null `hr_coil_id`, old `traceBabyCoilId`) silently drop out of coil-level rollups but **do not crash** — the read views guard on the new keys and unresolved coils contribute 0 cost. Watch the `tolerance()` helper: it returns `{ok:true}` when an arg is falsy, so the weight cap compares `prospectiveWeight > coil.actualWeight*1.05` explicitly to avoid a zero-weight coil allowing unlimited bundling.

2026-06-10 | Dashboard | Redesigned Dashboard with full metric coverage | Added: period selector (7/30/90 days, all time, custom date range — reuses CoilToSlit filter pattern), 3 KPI rows (production flow, stock-in-hand with ₹ values, commercial incl. PO visibility), production-vs-dispatch trend chart (daily ≤31 days / weekly beyond), SKU-wise summary table (DataTable with search/sort + CSV export), per-coil stock report CSV export, expanded alerts (dispatch variance ±5%, bundle over-allocation, PO expiry ≤7 days), recent-activity feed across all 5 stages. Dashboard now receives `skus` and `purchaseOrders` props. | Stock value at every stage uses the mother-coil rate (costPrice ÷ actualWeight, ₹/T) traced via `baby.hrCoilId` — consistent with the proportionate-cost model, no density constants. Stock formulas mirror existing consumption logic: slit remaining = baby weight − Σ tube theoreticalWeight; WIP = produced pcs − bundled tubeCount per baby coil.

2026-04-08 | Stage 4 (Bundle Formation) | Redesigned Bundle Formation UI — removed summary cards and flat DataTable | Replaced with expandable accordion table (one row per bundle, click to expand source rows) and two-mode form: "Create New Bundle" (full form) and "Add Source to BND-X" (simplified, pre-filled). Added search/sort on accordion, "+ Add Source" button inside expanded rows. | Users think in bundles, not allocation rows. Grouping by bundle with expand/collapse is far more intuitive than a flat list of rows. Two-mode form reduces cognitive load by showing only relevant fields per action.

2026-04-08 | Stage 1 | Chemistry fields (Carbon, Mn, YS, Elongation) removed from Coil Inward | Fields were not needed for plant operations — chemistry specs managed outside this system | Keep Stage 1 lean; only fields used in downstream calculations or daily operations

2026-04-08 | Stage 1 | Coil Grade changed from dropdown (GRADES constant) to free text input | Operators enter grades not always in a fixed list; dropdown was too restrictive | Use free text with placeholder examples when the value set is open-ended

2026-04-08 | Stage 2 | Baby coil Cost Price now auto-calculated proportionally | Formula: `(Baby Width / Sum Widths) × Parent Cost Price`. Recalculates all siblings on add/edit/delete, same as weight | Cost must follow the same proportionate pattern as weight to maintain traceability

2026-04-08 | Stage 2/3 | Width sum validation now uses 3-color system | Green (≤100%), Yellow (100-105% — save allowed), Red (>105% — save blocked). Replaces the old single-color tolerance badge | Operators need clear visual feedback: green = safe, yellow = warning but ok, red = stop

2026-04-08 | Stage 3 | Width changed from auto-fetched to manual input | Tube widths can differ from slit width (various tube profiles). Width sum validated against baby coil width with same 3-color system | Auto-fetching width was incorrect assumption — tubes from same slit can have different widths

2026-04-08 | Build | Folder name "Pipes&Tubes" contains `&` which bash interprets as command separator | Use `node node_modules/vite/bin/vite.js` directly instead of `npx vite` or `npm run dev` when shell expansion is an issue | Avoid special characters (&, spaces) in project folder names for CLI compatibility

2026-04-08 | Stage 2 (Coil to Slit) | Proportionate weight recalculation — adding a new baby coil changes all siblings' weights | After any add/edit/delete of baby coils, recalculate ALL siblings for the same parent using the updated width distribution | This is by design: proportionate weight means the sum always equals parent actual weight. Every mutation must trigger a full sibling recalc.

2026-04-08 | Stage 4 (Bundle Formation) | Multi-coil bundles — a single bundle can contain tubes from multiple baby coils, each as separate rows | Bundle rows are keyed by (bundleId + babyCoilId). Bundle summary is computed by grouping rows by bundleId. All rows in a bundle must share the same SKU. | This is the most complex stage. Carry-forward logic: leftover pieces from one baby coil become the first allocation in the next bundle.

2026-04-08 | Storage | useStore hook must read fresh value from localStorage inside updater function | `const next = typeof v === 'function' ? v(S.get(key) ?? fallback) : v` — reads from storage, not stale closure | React setState closures can capture stale values. Always read from source of truth (localStorage) inside functional updates.

2026-04-08 | Validation | ±5% tolerance is the universal validation threshold across all stages | `tolerance(actual, expected)` utility returns `{ok, pct, label}` — green badge for 95-105%, red outside | Consistent tolerance function reused across all 5 stages. Never hardcode tolerance checks inline.

2026-08-05 | Production coil allocation | `coil_allocations[].weight` is NOT stored state — `resolveProductionWeights` (calc.js:99) recomputes it as `pieces × weightPerTube` on every read. `pieces` is the only load-bearing field. | Any fix or migration touching allocations must rewrite `pieces`. Writing corrected `weight` to the DB is silently reverted on next load. Also explains why Σ allocation weight == production output to within 0.2 T across 1,259 rows — it is an identity (Σ pieces = tubeCount), not a coincidence. | Diagnosed while investigating 123.3 T of baby-coil over-consumption (issue #99). Nothing validates the operator's manual pick against a coil's remaining capacity, so all pieces can land on one coil.

2026-08-05 | Production coil allocation | A FIFO replay CANNOT reconstruct historical coil attribution. Dry run (`scripts/coil-realloc-dryrun.mjs`) rejected the backfill. | 93 of 1,259 productions have zero eligible coils under the app's own width ±5 mm / thickness ±0.3 mm rules — the floor genuinely used coils outside them. 690 T ends up unplaceable, so baby-coil stock INFLATES 695 T → 1,290 T, and 957 of 1,251 productions get reassigned to an entirely different coil set. That is fabricating history, not correcting it. | Fix forward only. The which-coil-fed-which-production fact was never captured and is not recoverable. Backup tables `backup_productions_20260805` / `backup_dispatches_20260805` exist in Supabase if a future attempt is made.

2026-08-05 | Production coil eligibility | Coil-to-pipe thickness is a plant LOOKUP TABLE (`RM_TO_FG_THICKNESS`), not a symmetric tolerance. The old ±0.3 mm band was wrong in both directions. | The relation is asymmetric and many-to-many: 2.3 coil rolls 2.5 pipe but 2.5 never rolls 2.3; 3.0 rolls both 3.0 and 3.2; 2.2 rolls both 2.2 and 2.3; 3.7 rolls 3.8 and 4.0. The band admitted 2.6 coil → 2.5 pipe (never run) and could not express the one-to-many rows. | Wrong suggestions are why operators overrode the FIFO pick by hand, which is the root cause of the 123.3 T over-consumption in issue #99. Fixing the rule attacks the cause, not the symptom. An FG gauge absent from the sheet now yields no eligible coil rather than falling back to a band — silent fallback is what hid this.

2026-08-05 | Production allocation | A warn-only validation is not a validation. The form already flagged ">105% of capacity — allowed, but review the split" and saved anyway; 445 coils overfilled by 123.3 T through that exact path. | Over-capacity is now a hard save block (`canSave` includes `!over105`), paired with a "Fix split" action so the operator has a one-click way out rather than a dead end. Under-allocation still saves as `partial` — only the physically impossible case blocks. | Guardrails that can be clicked past get clicked past, every shift, for months. If a state is impossible, block it; if it is merely unusual, warn. The cap lives in `capAllocationRows` (pure, 9 tests) so the rule is testable away from the form.

2026-08-17 | Reports (PB MTD workbook) | A column that is true row-by-row can still be false when summed. Distributor × SKU On-hand repeats one plant-wide tonnage on every distributor waiting on that size — 39.3 T of `50x50x2.0` appears 5 times, summing to 196.5 T the plant does not have. | Ship the rows, suppress the total. The sheet carries no total row at all (its rows only exist where an order line named a SKU, so Pending/Invoiced would not tie to the Dashboard either) plus a caption naming the sharing. Tests assert the absence: no `/total/i` label in column A, and no cell in the On-hand column equal to the column sum or a region subtotal. | Reverses ADR-0002's "stays out of the report". Withholding the sheet did not remove the question, it moved it into hand-built spreadsheets — which is where screen and workbook actually diverge. The objection was to a total, not to the rows.

2026-08-17 | Tests (exceljs) | `worksheet.getColumn(n).values` is a SPARSE array — blank rows are holes, and `.map()` preserves holes, so a later `.find()` visits `undefined` and throws on `.includes`. | Filter before mapping: `ws.getColumn(1).values.filter(v => v != null).map(String)`. Same trap applies to `getRow().values`, which is also 1-based (hence the existing `.slice(1)` in the render tests). | Cost one failing assertion that looked like a missing caption when the caption was rendered correctly.

2026-08-17 | PB MTD distributor sheet | `Unmapped` is not only "a state nobody mapped". A distributor's state is derived from its own order and invoice lines, so one with an estimate but no activity yet has NO state — and lands in Unmapped carrying real Plan tonnage. | The Unmapped block is a real block with real totals, never a "rest" bucket that can be filtered out of a sum. Its region total can hold a Plan while its invoiced is zero. | Found by a failing test while building #104: the fixture assumed a plan-only distributor sat in its "obvious" region. It cannot — nothing in an estimate row carries a state. The tests now cover both roads into Unmapped.

2026-08-18 | Sales SKU drill-down / PB MTD workbook | A SKU row that carries only a *code* has nowhere to get its name from. 37 of the 172 ERP codes on the order book (`1140-13075-10078295` = RHS 60x40x1.20) have **no row in the SKU master** — they are ordered, never produced — so `skus.find(s => s.skuCode === code)?.description || code` fell straight through to printing the MM ID as the description, on screen and in the workbook's Distributor × SKU sheet. | `salesByDistributor` now puts a `description` on every SKU row (master first, else the order line's own text; dispatch lines carry none, so the first order line wins). App reads `r.description || skuDesc(r.skuCode)`; `reports.js` `skuLabel` derives the same `size x thickness` shape from that description — or from the canonical key — when no master row exists. `skuInventoryRows` got the mirror fix: `codeByKey` keeps the real MM ID out of the canonical key (`rhs|4923|60x40|1.20|6000`) leaking into the SKU Code column. | The order line always had the right text — the display just never looked at it. Every `|| code` fallback in a label is a latent "shows an ID to a human" bug; the fix is to carry the name alongside the key, not to widen the lookup. Production/dispatch codes are 100% present in the master, so only the order side was ever affected — which is why it surfaced on the Sales tab and the MTD download and nowhere else.

2026-08-18 | Sales drill-down / PB MTD workbook | "On-hand" renamed to **Free Stock**, and it now nets Confirmed off: `freeStock = plant on-hand − Σ Confirmed across every distributor`. Both terms stay plant-wide, so the figure is still identical on every distributor's row (ADR-0002 unchanged); it is NOT floored, so an over-committed size reads negative — 50x50x2.0 SHS shows 39.3 on-hand against 74.0 confirmed → −34.7. `Short by` deliberately keeps measuring against on-hand, so a row can show no shortfall beside a negative Free Stock. | The rename could not be done alone: Confirmed is the ERP's `Release Qty − Invoiced Qty`, and the ERP does not release until dispatch, so **Confirmed is 0 T across all 318 open order lines** while 2,512 T sits in Non-confirmed (272 of those lines carry Order *Status* = "Confirmed", worth 2,098 T — the same word meaning two different things). Free Stock therefore reads exactly as On-hand did today and only moves as orders get released. Raised with the plant, accepted knowingly: the definition targets the order book they are moving towards. | Two lessons. (1) Check that the field a rename depends on is actually populated before shipping the rename — a column called "Free" that never moves is worse than one called "On-hand". (2) The plant-level Dashboard already computed exactly this (`Free FG = Inventory − Reserved`, and Reserved is that same released-not-invoiced tonnage); the drill-down was the odd one out. When a name already exists elsewhere in the app, match its formula or you ship two numbers under one word. Open question, not settled here: whether Confirmed should follow the ERP's Order Status column instead — it feeds the Dashboard KPIs, Best Estimate % and three workbook sheets, so it needs its own before/after.

2026-08-18 | Daily reports (region split) | The PB MTD **workbook** gained regions on 17 Aug (#101/#102/#104/#105); the **text and WhatsApp report skills** did not, and nobody noticed for a day because both still verified green — they verified plant totals, and plant totals were never wrong. The daily message the plant actually reads was the last flat-national artefact left. | `buildRegionMtdSummary` (`reports.js`) + `scripts/region-mtd.mjs` feed both skills Invoiced MTD and Pending to serve per region. Computed through `salesByDistributor` / `distributorRegionResolver`, never re-derived in SQL (ADR-0003). Region lines must sum to the plant headline printed above them — asserted in JS *and* re-checked against the skill's Postgres aggregate as checks 5 and 6. | Two lessons. (1) A verification that only checks totals cannot see a *missing dimension* — the reports were provably correct and useless at the same time. (2) When a dimension lands in one artefact, the follow-up question is which other artefacts describe the same thing; the answer here was sitting in a `grep -i region` that returned nothing in either skill. Live check on ship: South carries 100% of invoicing (463.5 T MTD) while West holds 1,397 T of the 2,512 T pending book and has never been invoiced — invisible in the old format, the headline in the new one.

2026-08-18 | Scripts / module resolution | `src/lib/calc.js:9` imported `'../data/stateRegions'` with no extension. Vite and Vitest resolve that; **plain Node does not**. So every `scripts/*.mjs` importing `calc.js` died with `ERR_MODULE_NOT_FOUND` — `coil-realloc-dryrun.mjs` had been dead since the region work landed, with 309 tests passing over it the whole time. Found only when a new script tried to import `reports.js`. | Two one-word fixes (`'../data/stateRegions.js'`, `'./calc.js'`), plus `src/lib/module-resolution.test.js`, which spawns a real `node --input-type=module -e "await import(...)"` and asserts exit 0. Documented in `docs/ARCHITECTURE.md` as a rule: relative imports inside `src/lib` and `src/data` carry `.js`. | The test runner and the runtime are not the same environment, so a green suite proves nothing about code the suite never executes *the way production does*. Any file with two consumers on different resolvers needs a test per resolver — the guard has to spawn the real binary, because anything running inside Vitest inherits the resolver that hides the bug. Also worth noting the class: `scripts/` had no CI coverage at all, so its only failure signal was a human running one.

2026-08-18 | Scripts / servable orders | New `scripts/servable-orders.mjs` answers "which pending orders can we actually serve today, distributor-wise" — `servable = min(pending, plant on-hand)` per distributor × SKU, rendered as a WhatsApp message. Every figure comes from the SAME `salesByDistributor(orders, dispatches, month, skus, { productions })` call the Sales drill-down and the PB MTD workbook read, so it cannot disagree with the screen. | The report deliberately has **no plant servable total**. On-hand is unreserved (ADR-0002), so the same tonnage appears against every distributor waiting on that size and the column does not add up — 50x50x2.0 SHS has 39.3 T on the floor with 88.0 T queued against it across six distributors, and each of the six reads its full pending as servable. Suppressing the total is the same fix ADR-0002 applied to the workbook's Distributor × SKU sheet. Lines whose size is oversubscribed plant-wide carry a `⚠️`, and today that is most of them: 2,512 T pending against ~1,408 T of stock. | Two lessons. (1) The **egress policy blocked `hztblmccvvarmgxmunrp.supabase.co`** (403 from the agent proxy), so the script's normal PostgREST path could not run at all in this session; the workaround was a `--agg` mode that expands per-SKU *sums* pulled through the Supabase MCP back into row shapes, leaving every identity / SKU-key / stock rule in calc.js where ADR-0003 wants it. Aggregate only plain Σs — the moment you aggregate an identity you have written a second answer. (2) The aggregation was verifiable because the repo had **recorded real numbers**: ADR-0002's worked example (39.3 T on-hand, 40.0 pending / 0.7 short for NEW PASHCHIM) and its "2,512 T in Non-confirmed" both reproduced exactly, which caught nothing but proved the whole chain. Write the live figures into ADRs — they become the tie-out for the next person who cannot reach the database.

2026-08-18 | Servable orders / service area | The first cut of `scripts/servable-orders.mjs` reported **plant-wide** and was materially wrong: 275.7 T of the 638.6 T it called servable belonged to five **West** (all Maharashtra) distributors that this plant does not ship to. The plant only serves **South**. Fixed with `--serves South`, plus KERALA and PUDUCHERRY added to the state→region seed (six rows → eight) because Nippon ships there and an unmapped state is silently dropped by a service-area filter. | The filter lands on the **order book, before `salesByDistributor`** — not on its output. That function derives `allPending`, Free Stock and the contested flag by summing across every distributor it is handed, so filtering afterwards leaves South sizes reading "contested" because of West demand that was never competing for the stock. Proof it mattered: S G Enterprises went from **all five sizes flagged ⚠️ to none**. Excluded tonnage is stated in the message footer (1,397.0 T / 6 distributors), never dropped in silence, and a distributor excluded merely for an **unmapped** state raises a loud warning instead — `Unmapped` is a labelling gap, never a service-area decision. | Three lessons. (1) **The constraint that breaks a report can be absent from the schema entirely.** There is no plant column, no plant on a distributor, and the string "Nippon" appears nowhere in the repo — so no amount of reading the data model would have surfaced it. The signal was sitting in an earlier LEARNINGS entry ("West holds 1,397 T of the pending book and has *never been invoiced*"): a region with a full order book and zero invoicing is a service-area boundary, not a sales problem. Read the anomalies already written down before adding a dimension. (2) A filter is not cosmetic when downstream figures aggregate across the thing being filtered — decide *where* in the pipeline it lands, and justify it. (3) Adding two rows to the region seed broke **12 tests**, all because KERALA was the fixtures' canonical "real state, no mapping". Repointed at ODISHA. When a test needs an example of an *unmapped* thing, that example is a hostage to the next mapping — name it in a way that says so.

2026-08-18 | Schema migrations (supabase-setup.sql) | `alter table productions add column …` was written next to the other pipeline migrations, near the top of the file — but `productions` is not **created** until ~60 lines further down. Idempotent on an existing database, fatal on a fresh one. | Migrations moved to sit *after* the `create table` they touch. The file is one script run top to bottom in the Supabase SQL Editor, so its ordering is load-bearing in a way a per-migration tool would hide. | The trap is that the file has two audiences with opposite reading orders: an existing deployment only executes the `alter`s, so every test you can do by hand passes, while the case that breaks is the one nobody runs — a brand new database. Group a migration with its table, not with the other migrations.

2026-08-18 | E2E (Playwright) | `e2e/pipeline.spec.js` and `e2e/slitting-multi.spec.js` have been failing since the login gate shipped (July 2026) — all 7 specs, every run. They `page.goto('/')` and immediately look for the stage tabs; the app now renders `Sign in to continue` instead, so every locator times out. Verified identical on a clean checkout of `main`, so it is not a regression from the plant work. | Not fixed here — it needs either a real test credential or a `LoginGate` bypass in `--mode test`, and both are decisions about auth, not about #120. Recorded so the next ticket that claims "the existing specs pass unchanged" checks what "unchanged" currently means. | A guard nobody runs is not a guard. The specs were the stated regression net for the whole #117 multi-plant spec — "the existing pipeline and slitting specs must keep passing" is an acceptance criterion on three of its tickets — and they had been red for a month. Also worth noting: this image ships Chromium revision 1194 while `@playwright/test` 1.60 wants 1223, so `npx playwright test` fails with "Executable doesn't exist" before it ever reaches the app; run it through a throwaway config setting `launchOptions.executablePath: '/opt/pw-browsers/chromium'`.

2026-08-19 | Migrations / seed paths | Gating a backfill closes the door behind it — so every OTHER write path has to carry the value itself. `supabase-setup.sql`'s plant backfill fires only when the column does not yet exist (so a re-run can never re-stamp a row the app deliberately left Unattributed). Correct, but it turns two forgotten seed paths into permanent holes: `scripts/import-excel.mjs` emitted `create table coils/baby_coils/productions` with no `plant` and inserted plant-less rows, and `restore-baby-coils.sql` likewise. Either one run after the migration writes rows the backfill will never revisit. | Both now state their own plant. The importer stamps `IMPORT_PLANT = 'hyderabad'` (guarded against a rename by `plantById()` at startup) into all three row builders — `buildInsertSql` derives its columns from the row objects, so it flows into the emitted SQL and the upsert `do update set` for free — and its schema preamble gained the column plus `add column if not exists`. `restore-baby-coils.sql` carries `'hyderabad'` in all 61 tuples. | Two lessons. (1) "Idempotent" and "self-healing" are not the same property, and making a migration properly idempotent can *cost* you the self-healing one. Once a backfill is one-shot, ask what else inserts into that table — the answer here was two files that no test and no code path touches, found only by a reviewer grepping for the table names. (2) When editing 61 hand-curated data tuples, verify arity with a real quoted-string-aware splitter rather than by eye: assert every row has the same column count as its header AND the literal landed in the right slot. Both files are historical artefacts nobody runs weekly, which is exactly why a silent shift would have surfaced years later.

## 2026-08-19 — #126 Role and plant decide what a user sees

**A test that cannot run is not a test that passes.** `TESTING.md` carried a "known environment
blocker": Chromium could not be downloaded in the sandbox, so the Playwright specs were "authored
and discoverable" but never executed. This session's machine had a pre-provisioned Chromium at
`/opt/pw-browsers/chromium`, so they ran for the first time in a long while — and **all 7 failed**,
none of it caused by this ticket:

| The spec still did | Reality |
|---|---|
| Filled `Cost Price (₹)` at Coil Inward | The field was deleted; cost arrives via the daily Excel |
| Clicked `Upload Dispatch Excel` | Dispatch has no upload — it is rebuilt from the Orders tab's workbook |
| `selectOption` on the SKU and HR Coil ID pickers | Both are `SearchSelect` (an `<input>` + option buttons), not `<select>` |
| Expected `Save Baby Coil` | The button counts: `Save 1 Baby Coil` |
| Expected `No eligible baby coil` | The message reads `No eligible coil to suggest` |
| Expected FIFO to allocate on its own | **Use suggestion** must be clicked — the never-auto-save rule |
| Went straight to `page.goto('/')` | The login gate has stood in front of the app since July |

Every one of those is a change that shipped, correctly, past a suite that could not object. The fix
is two lines in `playwright.config.js` — an optional `PW_CHROMIUM_PATH` that sets
`launchOptions.executablePath` — so a machine with a browser already on it never has to pretend it
cannot test. **Before believing a suite, run it.** 28 E2E tests now pass.

**Stub the backend, don't wait for it to fail.** With `.env.test` pointing at a host that does not
resolve, the app sat on "Loading inventory data..." while six table reads died slowly through the
proxy — longer than any test timeout. Answering `**/rest/v1/**` with `[]` is the same starting state
the specs always assumed, and instant. One subtlety worth keeping: answer **writes** as accepted
too. A rejected write makes `useSupabaseStore` re-pull so the UI stops showing rows Postgres
refused, and a re-pull against an empty stub would throw away the very coil the test just registered.

**A session can be well-formed and still grant nothing.** `parseStoredSession` asks "is this a
session?" and `accessFor` asks "what may it do?" — and a `plant` credential with a NULL plant column
passes the first and fails the second, because `verifyLoginDetails` maps NULL → `ALL_PLANTS` (right
for an admin, meaningless for a plant login). Keeping them separate was correct; requiring **both**
at the two call sites is what closes the hole. Reading that session as "every plant" would have
handed a plant login the whole company; signing them into an app with zero tabs would have
presented a credential to fix as a bug in the app. It now says which, and where it is fixed.

**Two lists of tabs is one list too many.** `APP_TABS` moved from `App.jsx` into `calc.js` beside
`accessFor`. A tab added to a component-local list and not to the rule is a tab shown to everyone by
a rule that had never heard of it — and `App.jsx` cannot be imported by the test suite, so no test
could ever catch the drift.

**Copy has to survive the change it describes.** The Dispatch plant-filter notice ended "Switch to
**All Plants** to delete" — an instruction a plant user cannot follow, because they have no
selector. Gating tabs meant re-reading every string that assumed the reader could change the scope.
`docs/ARCHITECTURE.md` also still described a "Reset Data" header button that no longer exists
anywhere in the app.

### Review round on #126 — what the two axes caught

**The doc you skip is the one that governs the code you changed.** `CLAUDE.md`'s "Read before you
change these areas" table binds `docs/UI-PATTERNS.md` to "Components, DataTable, stage forms". This
ticket changed a stage form (Coil Inward's Plant field), two components' write controls and a
DataTable's Actions column — and updated five other docs while skipping that one. Three of its
statements were left false. Updating the docs you *thought of* is not the same as updating the ones
the table names.

**"All Plants" is the worst possible fallback for a scoped user.** `plantLabelForHeader` ended
`|| 'All Plants'`. For an admin that is unreachable (the value always comes from the options). For a
plant user whose credential names a plant id no master row matches, it fired — and the one place
their plant is stated told them they were looking at the whole company. A fallback is only safe if
it is safe on *every* path that can reach it. It now shows the unmatched id itself.

**Unmounting a button is not withholding the feature.** The Orders upload's hidden
`<input type="file">` was left mounted while only its `<Btn>` was gated — the whole write path one
console line away, and the docs claiming controls were "withheld from the DOM". The trigger and the
thing it triggers gate together.

**Two mechanisms documented as separate will diverge — gate on the right one.** `accessFor` gated
the manufacturing tabs on `manufactures`, but Coil Inward's admin picker honours
`COIL_INWARD_PLANT_IDS`, a rollout list `calc.js` explicitly says is *not* the same question. A
plant with `manufactures: true` not yet on the rollout list would have let a plant user register
coils an admin cannot offer. Latent today (both lists match) — closed, with a test that flips
Lepakshi to manufacturing to prove it.

**Scoping hides the unattributed, and someone must still fix them.** `filterByPlant` matches the
stored value exactly, so a legacy blank-plant row is invisible to a plant user in the pipeline
stages. That is right — a row that cannot say where it sits is not one plant's to claim — but it
silently makes backfilling an admin-only job. Written down in both `ARCHITECTURE.md` and
`UI-PATTERNS.md` rather than left to be discovered by a plant user who cannot find their coil.

### Round two on #126 — the fix that was worse than the bug

**Never hand a component a filtered copy of a store it writes back.** Scoping the pipeline stages by
passing `pipelineBabyCoils` looked like the tidy version of "a plant user sees only their plant". It
was a data-loss bug. `Slitting.save` builds its next array from the `babyCoils` prop and calls
`setBabyCoils(updated)` — not `setBabyCoils(prev => …)` — so with a filtered prop `next` is a strict
subset of `prev`, and `syncToSupabase` reads every id in prev-but-not-next as a deletion. On
`baby_coils`, a HARD one. **One Hyderabad operator saving one baby coil would have permanently
deleted every NPMD baby coil.** `CoilInward` and `Production` use the functional setter and were
safe — which is exactly why the bug was easy to miss: two of the three stages tolerated it.

The same blunt filtering also silently disarmed the cross-stage guards. "A baby coil consumed by
**any** production cannot be deleted" is only true against the whole register; so are the duplicate
`hrCoilId` check and `nextCoilNumber`. Worst case is a legacy blank-plant row, which `genHRCoilId`
gives an `HYD-` id — precisely what a new Hyderabad coil collides with, and precisely what the
scoped array hid. **Display scopes; state and guards do not.**

**A regression test is not finished until you have watched it fail.** The first version of the
new test passed with the defect deliberately reintroduced — twice, for two different reasons:
1. **Route shadowing.** The recorder installed its own `page.route` before `signIn` installed the
   stub. Playwright runs the most recently registered handler first, so the recorder never fired and
   `writes` was always `[]` — every assertion on it was vacuously true. Fixed by passing `onRequest`
   into `signIn` (one handler), plus an `expect(writes.length).toBeGreaterThan(0)` so a silent
   recorder fails loudly instead of passing quietly.
2. **Ordering.** `syncToSupabase` awaits its upsert before issuing any delete, so when the saved row
   appears on screen the destructive half of that same sync has not been sent. Asserting there
   passes while the bug is live. Fixed with a `settle()` that waits for the request traffic to stop.

Only after reintroducing the defect a third time did the test actually go red. Two review rounds
found this; the first round's Standards axis flagged the guard narrowing and the Spec axis found the
delete. **Neither would have been caught by the app's own screens** — the rows destroyed are the
ones the user cannot see, which is why the assertion had to move to the network layer.

## 2026-08-20 — A rule in prose, contradicted by three captions, is not a rule

**West distributors were shown South stock for as long as the sheet has existed.** `producedPool`
summed every plant's tonnage by SKU and never read `p.plant`; `salesByDistributor` wrote that one
number onto every distributor's row. On the 20-Aug-2026 workbook that offered 310.61 MT of Hyderabad
tonnage across 50 West rows and printed West's shortfall as **1,755.35 MT** against a true
**2,116 MT** — the entire West order book.

The rule was not missing. It was in `CONTEXT.md` under **Service area**, and implemented once, as
the `--serves` flag in `scripts/servable-orders.mjs` — which filters *orders*, never *stock*. What
kept it invisible was that **three captions asserted the opposite as if it were a decision**: the
workbook's sheet-4 note ("Free Stock is every plant's finished stock combined — the plant column is
not applied to it, because stock is held where it was made"), and two lines of `docs/ALGORITHMS.md`
saying the same. Every review since read those, found them coherent, and moved on.

**A business rule that lives in one prose document and one CLI flag is a rule nobody enforces.** The
fix was to make it data — a `serves` column on a plant master — so the code has to read it. Prose
can be contradicted by a caption; a `Set` of plant ids cannot.

**Scoping half of a subtraction is worse than scoping none of it.** `onhand` is produced − invoiced.
Filtering productions to West (nothing) while leaving dispatches national would have made every West
SKU read Hyderabad's invoiced tonnage as **negative stock** — a screen full of red where the truth is
a blank. Four things had to move in one commit: productions, dispatches, `allConfirmed`,
`allPending`. Two more, and less obvious: `regionOf` had to be resolved *above* the stock block (it
ran after, which is one reason the pool could only ever be global), and the `Unmapped` case had to
stop being coerced with `?? 0`.

**"No filter" and "an empty filter" are opposite instructions, and one sentinel cannot say both.**
`filterByPlant(rows, ALL_PLANTS)` returns everything. The new `filterByPlants(rows, new Set())` must
return **nothing** — a region no plant serves has no stock, and falling back to "everything" there
is precisely the bug, at the exact moment it matters most. So the set-based siblings use `null` for
"do not filter", which cannot be confused with an empty set.

**Unknown is not empty.** An `Unmapped` distributor has no derivable service area. Printing `0`
tells a sales team "we hold nothing for you"; printing `?` tells them "nobody has mapped your
state". Those are opposite instructions and one of them is false. `reports.js` was coercing with
`?? 0` — one operator, three characters, a wrong answer every day.

**Fixtures that carry none of the dimension under test pass for the wrong reason.** Both stock
fixtures had no `plant` on their productions and — in `calc.test.js` — no `shipToState` either.
Adding the service-area code turned every one of those tests green while proving nothing: with no
plant the stock belongs to no area, so *everything* reads zero and every assertion about zero holds.
The dimension had to be added to the fixtures **before** the assertions were flipped, and the
over-dispatch case needed `plant` on the dispatch entry too, or the filter drops the row and the
floor at zero is never exercised.
